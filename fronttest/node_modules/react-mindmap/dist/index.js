(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory(require("react"));
	else if(typeof define === 'function' && define.amd)
		define(["react"], factory);
	else if(typeof exports === 'object')
		exports["MindMap"] = factory(require("react"));
	else
		root["MindMap"] = factory(root["React"]);
})(typeof self !== 'undefined' ? self : this, function(__WEBPACK_EXTERNAL_MODULE_7__) {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 5);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


// shim for using process in browser
var process = module.exports = {};

// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.

var cachedSetTimeout;
var cachedClearTimeout;

function defaultSetTimout() {
    throw new Error('setTimeout has not been defined');
}
function defaultClearTimeout() {
    throw new Error('clearTimeout has not been defined');
}
(function () {
    try {
        if (typeof setTimeout === 'function') {
            cachedSetTimeout = setTimeout;
        } else {
            cachedSetTimeout = defaultSetTimout;
        }
    } catch (e) {
        cachedSetTimeout = defaultSetTimout;
    }
    try {
        if (typeof clearTimeout === 'function') {
            cachedClearTimeout = clearTimeout;
        } else {
            cachedClearTimeout = defaultClearTimeout;
        }
    } catch (e) {
        cachedClearTimeout = defaultClearTimeout;
    }
})();
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) {
        //normal enviroments in sane situations
        return setTimeout(fun, 0);
    }
    // if setTimeout wasn't available but was latter defined
    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
        cachedSetTimeout = setTimeout;
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch (e) {
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch (e) {
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }
}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) {
        //normal enviroments in sane situations
        return clearTimeout(marker);
    }
    // if clearTimeout wasn't available but was latter defined
    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
        cachedClearTimeout = clearTimeout;
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e) {
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e) {
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }
}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;

function cleanUpNextTick() {
    if (!draining || !currentQueue) {
        return;
    }
    draining = false;
    if (currentQueue.length) {
        queue = currentQueue.concat(queue);
    } else {
        queueIndex = -1;
    }
    if (queue.length) {
        drainQueue();
    }
}

function drainQueue() {
    if (draining) {
        return;
    }
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;

    var len = queue.length;
    while (len) {
        currentQueue = queue;
        queue = [];
        while (++queueIndex < len) {
            if (currentQueue) {
                currentQueue[queueIndex].run();
            }
        }
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}

process.nextTick = function (fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) {
        for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
        }
    }
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) {
        runTimeout(drainQueue);
    }
};

// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function () {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;
process.prependListener = noop;
process.prependOnceListener = noop;

process.listeners = function (name) {
    return [];
};

process.binding = function (name) {
    throw new Error('process.binding is not supported');
};

process.cwd = function () {
    return '/';
};
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function () {
    return 0;
};

/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';

module.exports = ReactPropTypesSecret;

/***/ }),
/* 2 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
var __WEBPACK_AMD_DEFINE_FACTORY__, __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

!function (t, n) {
  "object" == ( false ? "undefined" : _typeof(exports)) && "undefined" != typeof module ? n(exports) :  true ? !(__WEBPACK_AMD_DEFINE_ARRAY__ = [exports], __WEBPACK_AMD_DEFINE_FACTORY__ = (n),
				__WEBPACK_AMD_DEFINE_RESULT__ = (typeof __WEBPACK_AMD_DEFINE_FACTORY__ === 'function' ?
				(__WEBPACK_AMD_DEFINE_FACTORY__.apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__)) : __WEBPACK_AMD_DEFINE_FACTORY__),
				__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__)) : n(t.d3 = {});
}(undefined, function (D) {
  "use strict";
  var o = { value: function value() {} };function S() {
    for (var t, n = 0, e = arguments.length, r = {}; n < e; ++n) {
      if (!(t = arguments[n] + "") || t in r) throw new Error("illegal type: " + t);r[t] = [];
    }return new i(r);
  }function i(t) {
    this._ = t;
  }function c(t, n) {
    for (var e, r = 0, i = t.length; r < i; ++r) {
      if ((e = t[r]).name === n) return e.value;
    }
  }function f(t, n, e) {
    for (var r = 0, i = t.length; r < i; ++r) {
      if (t[r].name === n) {
        t[r] = o, t = t.slice(0, r).concat(t.slice(r + 1));break;
      }
    }return null != e && t.push({ name: n, value: e }), t;
  }i.prototype = S.prototype = { constructor: i, on: function on(t, n) {
      var e,
          r,
          i = this._,
          o = (r = i, (t + "").trim().split(/^|\s+/).map(function (t) {
        var n = "",
            e = t.indexOf(".");if (0 <= e && (n = t.slice(e + 1), t = t.slice(0, e)), t && !r.hasOwnProperty(t)) throw new Error("unknown type: " + t);return { type: t, name: n };
      })),
          a = -1,
          u = o.length;if (!(arguments.length < 2)) {
        if (null != n && "function" != typeof n) throw new Error("invalid callback: " + n);for (; ++a < u;) {
          if (e = (t = o[a]).type) i[e] = f(i[e], t.name, n);else if (null == n) for (e in i) {
            i[e] = f(i[e], t.name, null);
          }
        }return this;
      }for (; ++a < u;) {
        if ((e = (t = o[a]).type) && (e = c(i[e], t.name))) return e;
      }
    }, copy: function copy() {
      var t = {},
          n = this._;for (var e in n) {
        t[e] = n[e].slice();
      }return new i(t);
    }, call: function call(t, n) {
      if (0 < (e = arguments.length - 2)) for (var e, r, i = new Array(e), o = 0; o < e; ++o) {
        i[o] = arguments[o + 2];
      }if (!this._.hasOwnProperty(t)) throw new Error("unknown type: " + t);for (o = 0, e = (r = this._[t]).length; o < e; ++o) {
        r[o].value.apply(n, i);
      }
    }, apply: function apply(t, n, e) {
      if (!this._.hasOwnProperty(t)) throw new Error("unknown type: " + t);for (var r = this._[t], i = 0, o = r.length; i < o; ++i) {
        r[i].value.apply(n, e);
      }
    } };var r = "http://www.w3.org/1999/xhtml",
      a = { svg: "http://www.w3.org/2000/svg", xhtml: r, xlink: "http://www.w3.org/1999/xlink", xml: "http://www.w3.org/XML/1998/namespace", xmlns: "http://www.w3.org/2000/xmlns/" },
      u = function u(t) {
    var n = t += "",
        e = n.indexOf(":");return 0 <= e && "xmlns" !== (n = t.slice(0, e)) && (t = t.slice(e + 1)), a.hasOwnProperty(n) ? { space: a[n], local: t } : t;
  };var s = function s(t) {
    var n = u(t);return (n.local ? function (t) {
      return function () {
        return this.ownerDocument.createElementNS(t.space, t.local);
      };
    } : function (e) {
      return function () {
        var t = this.ownerDocument,
            n = this.namespaceURI;return n === r && t.documentElement.namespaceURI === r ? t.createElement(e) : t.createElementNS(n, e);
      };
    })(n);
  };function n() {}var d = function d(t) {
    return null == t ? n : function () {
      return this.querySelector(t);
    };
  };function e() {
    return [];
  }var g = function g(t) {
    return null == t ? e : function () {
      return this.querySelectorAll(t);
    };
  },
      t = function t(_t2) {
    return function () {
      return this.matches(_t2);
    };
  };if ("undefined" != typeof document) {
    var h = document.documentElement;if (!h.matches) {
      var l = h.webkitMatchesSelector || h.msMatchesSelector || h.mozMatchesSelector || h.oMatchesSelector;t = function t(_t3) {
        return function () {
          return l.call(this, _t3);
        };
      };
    }
  }var p = t,
      v = function v(t) {
    return new Array(t.length);
  };function y(t, n) {
    this.ownerDocument = t.ownerDocument, this.namespaceURI = t.namespaceURI, this._next = null, this._parent = t, this.__data__ = n;
  }y.prototype = { constructor: y, appendChild: function appendChild(t) {
      return this._parent.insertBefore(t, this._next);
    }, insertBefore: function insertBefore(t, n) {
      return this._parent.insertBefore(t, n);
    }, querySelector: function querySelector(t) {
      return this._parent.querySelector(t);
    }, querySelectorAll: function querySelectorAll(t) {
      return this._parent.querySelectorAll(t);
    } };function x(t, n, e, r, i, o) {
    for (var a, u = 0, c = n.length, f = o.length; u < f; ++u) {
      (a = n[u]) ? (a.__data__ = o[u], r[u] = a) : e[u] = new y(t, o[u]);
    }for (; u < c; ++u) {
      (a = n[u]) && (i[u] = a);
    }
  }function M(t, n, e, r, i, o, a) {
    var u,
        c,
        f,
        s = {},
        h = n.length,
        l = o.length,
        d = new Array(h);for (u = 0; u < h; ++u) {
      (c = n[u]) && (d[u] = f = "$" + a.call(c, c.__data__, u, n), f in s ? i[u] = c : s[f] = c);
    }for (u = 0; u < l; ++u) {
      (c = s[f = "$" + a.call(t, o[u], u, o)]) ? ((r[u] = c).__data__ = o[u], s[f] = null) : e[u] = new y(t, o[u]);
    }for (u = 0; u < h; ++u) {
      (c = n[u]) && s[d[u]] === c && (i[u] = c);
    }
  }function b(t, n) {
    return t < n ? -1 : n < t ? 1 : n <= t ? 0 : NaN;
  }var m = function m(t) {
    return t.ownerDocument && t.ownerDocument.defaultView || t.document && t || t.defaultView;
  };function w(t, n) {
    return t.style.getPropertyValue(n) || m(t).getComputedStyle(t, null).getPropertyValue(n);
  }function _(t) {
    return t.trim().split(/^|\s+/);
  }function T(t) {
    return t.classList || new N(t);
  }function N(t) {
    this._node = t, this._names = _(t.getAttribute("class") || "");
  }function C(t, n) {
    for (var e = T(t), r = -1, i = n.length; ++r < i;) {
      e.add(n[r]);
    }
  }function k(t, n) {
    for (var e = T(t), r = -1, i = n.length; ++r < i;) {
      e.remove(n[r]);
    }
  }N.prototype = { add: function add(t) {
      this._names.indexOf(t) < 0 && (this._names.push(t), this._node.setAttribute("class", this._names.join(" ")));
    }, remove: function remove(t) {
      var n = this._names.indexOf(t);0 <= n && (this._names.splice(n, 1), this._node.setAttribute("class", this._names.join(" ")));
    }, contains: function contains(t) {
      return 0 <= this._names.indexOf(t);
    } };function A() {
    this.textContent = "";
  }function U() {
    this.innerHTML = "";
  }function z() {
    this.nextSibling && this.parentNode.appendChild(this);
  }function E() {
    this.previousSibling && this.parentNode.insertBefore(this, this.parentNode.firstChild);
  }function Y() {
    return null;
  }function F() {
    var t = this.parentNode;t && t.removeChild(this);
  }function q() {
    return this.parentNode.insertBefore(this.cloneNode(!1), this.nextSibling);
  }function H() {
    return this.parentNode.insertBefore(this.cloneNode(!0), this.nextSibling);
  }var P = {};(D.event = null, "undefined" != typeof document) && ("onmouseenter" in document.documentElement || (P = { mouseenter: "mouseover", mouseleave: "mouseout" }));function X(e, t, n) {
    return e = L(e, t, n), function (t) {
      var n = t.relatedTarget;n && (n === this || 8 & n.compareDocumentPosition(this)) || e.call(this, t);
    };
  }function L(e, r, i) {
    return function (t) {
      var n = D.event;D.event = t;try {
        e.call(this, this.__data__, r, i);
      } finally {
        D.event = n;
      }
    };
  }function j(o) {
    return function () {
      var t = this.__on;if (t) {
        for (var n, e = 0, r = -1, i = t.length; e < i; ++e) {
          n = t[e], o.type && n.type !== o.type || n.name !== o.name ? t[++r] = n : this.removeEventListener(n.type, n.listener, n.capture);
        }++r ? t.length = r : delete this.__on;
      }
    };
  }function O(c, f, s) {
    var h = P.hasOwnProperty(c.type) ? X : L;return function (t, n, e) {
      var r,
          i = this.__on,
          o = h(f, n, e);if (i) for (var a = 0, u = i.length; a < u; ++a) {
        if ((r = i[a]).type === c.type && r.name === c.name) return this.removeEventListener(r.type, r.listener, r.capture), this.addEventListener(r.type, r.listener = o, r.capture = s), void (r.value = f);
      }this.addEventListener(c.type, o, s), r = { type: c.type, name: c.name, value: f, listener: o, capture: s }, i ? i.push(r) : this.__on = [r];
    };
  }function I(t, n, e, r) {
    var i = D.event;t.sourceEvent = D.event, D.event = t;try {
      return n.apply(e, r);
    } finally {
      D.event = i;
    }
  }function V(t, n, e) {
    var r = m(t),
        i = r.CustomEvent;"function" == typeof i ? i = new i(n, e) : (i = r.document.createEvent("Event"), e ? (i.initEvent(n, e.bubbles, e.cancelable), i.detail = e.detail) : i.initEvent(n, !1, !1)), t.dispatchEvent(i);
  }var R = [null];function B(t, n) {
    this._groups = t, this._parents = n;
  }function $() {
    return new B([[document.documentElement]], R);
  }B.prototype = $.prototype = { constructor: B, select: function select(t) {
      "function" != typeof t && (t = d(t));for (var n = this._groups, e = n.length, r = new Array(e), i = 0; i < e; ++i) {
        for (var o, a, u = n[i], c = u.length, f = r[i] = new Array(c), s = 0; s < c; ++s) {
          (o = u[s]) && (a = t.call(o, o.__data__, s, u)) && ("__data__" in o && (a.__data__ = o.__data__), f[s] = a);
        }
      }return new B(r, this._parents);
    }, selectAll: function selectAll(t) {
      "function" != typeof t && (t = g(t));for (var n = this._groups, e = n.length, r = [], i = [], o = 0; o < e; ++o) {
        for (var a, u = n[o], c = u.length, f = 0; f < c; ++f) {
          (a = u[f]) && (r.push(t.call(a, a.__data__, f, u)), i.push(a));
        }
      }return new B(r, i);
    }, filter: function filter(t) {
      "function" != typeof t && (t = p(t));for (var n = this._groups, e = n.length, r = new Array(e), i = 0; i < e; ++i) {
        for (var o, a = n[i], u = a.length, c = r[i] = [], f = 0; f < u; ++f) {
          (o = a[f]) && t.call(o, o.__data__, f, a) && c.push(o);
        }
      }return new B(r, this._parents);
    }, data: function data(t, n) {
      if (!t) return p = new Array(this.size()), s = -1, this.each(function (t) {
        p[++s] = t;
      }), p;var e,
          r = n ? M : x,
          i = this._parents,
          o = this._groups;"function" != typeof t && (e = t, t = function t() {
        return e;
      });for (var a = o.length, u = new Array(a), c = new Array(a), f = new Array(a), s = 0; s < a; ++s) {
        var h = i[s],
            l = o[s],
            d = l.length,
            p = t.call(h, h && h.__data__, s, i),
            v = p.length,
            y = c[s] = new Array(v),
            g = u[s] = new Array(v);r(h, l, y, g, f[s] = new Array(d), p, n);for (var b, m, _ = 0, w = 0; _ < v; ++_) {
          if (b = y[_]) {
            for (w <= _ && (w = _ + 1); !(m = g[w]) && ++w < v;) {}b._next = m || null;
          }
        }
      }return (u = new B(u, i))._enter = c, u._exit = f, u;
    }, enter: function enter() {
      return new B(this._enter || this._groups.map(v), this._parents);
    }, exit: function exit() {
      return new B(this._exit || this._groups.map(v), this._parents);
    }, merge: function merge(t) {
      for (var n = this._groups, e = t._groups, r = n.length, i = e.length, o = Math.min(r, i), a = new Array(r), u = 0; u < o; ++u) {
        for (var c, f = n[u], s = e[u], h = f.length, l = a[u] = new Array(h), d = 0; d < h; ++d) {
          (c = f[d] || s[d]) && (l[d] = c);
        }
      }for (; u < r; ++u) {
        a[u] = n[u];
      }return new B(a, this._parents);
    }, order: function order() {
      for (var t = this._groups, n = -1, e = t.length; ++n < e;) {
        for (var r, i = t[n], o = i.length - 1, a = i[o]; 0 <= --o;) {
          (r = i[o]) && (a && a !== r.nextSibling && a.parentNode.insertBefore(r, a), a = r);
        }
      }return this;
    }, sort: function sort(e) {
      function t(t, n) {
        return t && n ? e(t.__data__, n.__data__) : !t - !n;
      }e || (e = b);for (var n = this._groups, r = n.length, i = new Array(r), o = 0; o < r; ++o) {
        for (var a, u = n[o], c = u.length, f = i[o] = new Array(c), s = 0; s < c; ++s) {
          (a = u[s]) && (f[s] = a);
        }f.sort(t);
      }return new B(i, this._parents).order();
    }, call: function call() {
      var t = arguments[0];return arguments[0] = this, t.apply(null, arguments), this;
    }, nodes: function nodes() {
      var t = new Array(this.size()),
          n = -1;return this.each(function () {
        t[++n] = this;
      }), t;
    }, node: function node() {
      for (var t = this._groups, n = 0, e = t.length; n < e; ++n) {
        for (var r = t[n], i = 0, o = r.length; i < o; ++i) {
          var a = r[i];if (a) return a;
        }
      }return null;
    }, size: function size() {
      var t = 0;return this.each(function () {
        ++t;
      }), t;
    }, empty: function empty() {
      return !this.node();
    }, each: function each(t) {
      for (var n = this._groups, e = 0, r = n.length; e < r; ++e) {
        for (var i, o = n[e], a = 0, u = o.length; a < u; ++a) {
          (i = o[a]) && t.call(i, i.__data__, a, o);
        }
      }return this;
    }, attr: function attr(t, n) {
      var e = u(t);if (arguments.length < 2) {
        var r = this.node();return e.local ? r.getAttributeNS(e.space, e.local) : r.getAttribute(e);
      }return this.each((null == n ? e.local ? function (t) {
        return function () {
          this.removeAttributeNS(t.space, t.local);
        };
      } : function (t) {
        return function () {
          this.removeAttribute(t);
        };
      } : "function" == typeof n ? e.local ? function (n, e) {
        return function () {
          var t = e.apply(this, arguments);null == t ? this.removeAttributeNS(n.space, n.local) : this.setAttributeNS(n.space, n.local, t);
        };
      } : function (n, e) {
        return function () {
          var t = e.apply(this, arguments);null == t ? this.removeAttribute(n) : this.setAttribute(n, t);
        };
      } : e.local ? function (t, n) {
        return function () {
          this.setAttributeNS(t.space, t.local, n);
        };
      } : function (t, n) {
        return function () {
          this.setAttribute(t, n);
        };
      })(e, n));
    }, style: function style(t, n, e) {
      return 1 < arguments.length ? this.each((null == n ? function (t) {
        return function () {
          this.style.removeProperty(t);
        };
      } : "function" == typeof n ? function (n, e, r) {
        return function () {
          var t = e.apply(this, arguments);null == t ? this.style.removeProperty(n) : this.style.setProperty(n, t, r);
        };
      } : function (t, n, e) {
        return function () {
          this.style.setProperty(t, n, e);
        };
      })(t, n, null == e ? "" : e)) : w(this.node(), t);
    }, property: function property(t, n) {
      return 1 < arguments.length ? this.each((null == n ? function (t) {
        return function () {
          delete this[t];
        };
      } : "function" == typeof n ? function (n, e) {
        return function () {
          var t = e.apply(this, arguments);null == t ? delete this[n] : this[n] = t;
        };
      } : function (t, n) {
        return function () {
          this[t] = n;
        };
      })(t, n)) : this.node()[t];
    }, classed: function classed(t, n) {
      var e = _(t + "");if (arguments.length < 2) {
        for (var r = T(this.node()), i = -1, o = e.length; ++i < o;) {
          if (!r.contains(e[i])) return !1;
        }return !0;
      }return this.each(("function" == typeof n ? function (t, n) {
        return function () {
          (n.apply(this, arguments) ? C : k)(this, t);
        };
      } : n ? function (t) {
        return function () {
          C(this, t);
        };
      } : function (t) {
        return function () {
          k(this, t);
        };
      })(e, n));
    }, text: function text(t) {
      return arguments.length ? this.each(null == t ? A : ("function" == typeof t ? function (n) {
        return function () {
          var t = n.apply(this, arguments);this.textContent = null == t ? "" : t;
        };
      } : function (t) {
        return function () {
          this.textContent = t;
        };
      })(t)) : this.node().textContent;
    }, html: function html(t) {
      return arguments.length ? this.each(null == t ? U : ("function" == typeof t ? function (n) {
        return function () {
          var t = n.apply(this, arguments);this.innerHTML = null == t ? "" : t;
        };
      } : function (t) {
        return function () {
          this.innerHTML = t;
        };
      })(t)) : this.node().innerHTML;
    }, raise: function raise() {
      return this.each(z);
    }, lower: function lower() {
      return this.each(E);
    }, append: function append(t) {
      var n = "function" == typeof t ? t : s(t);return this.select(function () {
        return this.appendChild(n.apply(this, arguments));
      });
    }, insert: function insert(t, n) {
      var e = "function" == typeof t ? t : s(t),
          r = null == n ? Y : "function" == typeof n ? n : d(n);return this.select(function () {
        return this.insertBefore(e.apply(this, arguments), r.apply(this, arguments) || null);
      });
    }, remove: function remove() {
      return this.each(F);
    }, clone: function clone(t) {
      return this.select(t ? H : q);
    }, datum: function datum(t) {
      return arguments.length ? this.property("__data__", t) : this.node().__data__;
    }, on: function on(t, n, e) {
      var r,
          i,
          o = (t + "").trim().split(/^|\s+/).map(function (t) {
        var n = "",
            e = t.indexOf(".");return 0 <= e && (n = t.slice(e + 1), t = t.slice(0, e)), { type: t, name: n };
      }),
          a = o.length;if (!(arguments.length < 2)) {
        for (u = n ? O : j, null == e && (e = !1), r = 0; r < a; ++r) {
          this.each(u(o[r], n, e));
        }return this;
      }var u = this.node().__on;if (u) for (var c, f = 0, s = u.length; f < s; ++f) {
        for (r = 0, c = u[f]; r < a; ++r) {
          if ((i = o[r]).type === c.type && i.name === c.name) return c.value;
        }
      }
    }, dispatch: function dispatch(t, n) {
      return this.each(("function" == typeof n ? function (t, n) {
        return function () {
          return V(this, t, n.apply(this, arguments));
        };
      } : function (t, n) {
        return function () {
          return V(this, t, n);
        };
      })(t, n));
    } };var Z = function Z(t) {
    return "string" == typeof t ? new B([[document.querySelector(t)]], [document.documentElement]) : new B([[t]], R);
  },
      W = function W() {
    for (var t, n = D.event; t = n.sourceEvent;) {
      n = t;
    }return n;
  },
      Q = function Q(t, n) {
    var e = t.ownerSVGElement || t;if (e.createSVGPoint) {
      var r = e.createSVGPoint();return r.x = n.clientX, r.y = n.clientY, [(r = r.matrixTransform(t.getScreenCTM().inverse())).x, r.y];
    }var i = t.getBoundingClientRect();return [n.clientX - i.left - t.clientLeft, n.clientY - i.top - t.clientTop];
  },
      J = function J(t) {
    var n = W();return n.changedTouches && (n = n.changedTouches[0]), Q(t, n);
  },
      G = function G(t, n, e) {
    arguments.length < 3 && (e = n, n = W().changedTouches);for (var r, i = 0, o = n ? n.length : 0; i < o; ++i) {
      if ((r = n[i]).identifier === e) return Q(t, r);
    }return null;
  };function K() {
    D.event.stopImmediatePropagation();
  }var tt = function tt() {
    D.event.preventDefault(), D.event.stopImmediatePropagation();
  },
      nt = function nt(t) {
    var n = t.document.documentElement,
        e = Z(t).on("dragstart.drag", tt, !0);"onselectstart" in n ? e.on("selectstart.drag", tt, !0) : (n.__noselect = n.style.MozUserSelect, n.style.MozUserSelect = "none");
  };function et(t, n) {
    var e = t.document.documentElement,
        r = Z(t).on("dragstart.drag", null);n && (r.on("click.drag", tt, !0), setTimeout(function () {
      r.on("click.drag", null);
    }, 0)), "onselectstart" in e ? r.on("selectstart.drag", null) : (e.style.MozUserSelect = e.__noselect, delete e.__noselect);
  }function rt(t, n, e, r, i, o, a, u, c, f) {
    this.target = t, this.type = n, this.subject = e, this.identifier = r, this.active = i, this.x = o, this.y = a, this.dx = u, this.dy = c, this._ = f;
  }function it() {
    return !D.event.button;
  }function ot() {
    return this.parentNode;
  }function at(t) {
    return null == t ? { x: D.event.x, y: D.event.y } : t;
  }function ut() {
    return "ontouchstart" in this;
  }rt.prototype.on = function () {
    var t = this._.on.apply(this._, arguments);return t === this._ ? this : t;
  };var ct = function ct(t, n, e) {
    t.prototype = n.prototype = e, e.constructor = t;
  };function ft(t, n) {
    var e = Object.create(t.prototype);for (var r in n) {
      e[r] = n[r];
    }return e;
  }function st() {}var ht = 1 / .7,
      lt = "\\s*([+-]?\\d+)\\s*",
      dt = "\\s*([+-]?\\d*\\.?\\d+(?:[eE][+-]?\\d+)?)\\s*",
      pt = "\\s*([+-]?\\d*\\.?\\d+(?:[eE][+-]?\\d+)?)%\\s*",
      vt = /^#([0-9a-f]{3})$/,
      yt = /^#([0-9a-f]{6})$/,
      gt = new RegExp("^rgb\\(" + [lt, lt, lt] + "\\)$"),
      bt = new RegExp("^rgb\\(" + [pt, pt, pt] + "\\)$"),
      mt = new RegExp("^rgba\\(" + [lt, lt, lt, dt] + "\\)$"),
      _t = new RegExp("^rgba\\(" + [pt, pt, pt, dt] + "\\)$"),
      wt = new RegExp("^hsl\\(" + [dt, pt, pt] + "\\)$"),
      xt = new RegExp("^hsla\\(" + [dt, pt, pt, dt] + "\\)$"),
      Mt = { aliceblue: 15792383, antiquewhite: 16444375, aqua: 65535, aquamarine: 8388564, azure: 15794175, beige: 16119260, bisque: 16770244, black: 0, blanchedalmond: 16772045, blue: 255, blueviolet: 9055202, brown: 10824234, burlywood: 14596231, cadetblue: 6266528, chartreuse: 8388352, chocolate: 13789470, coral: 16744272, cornflowerblue: 6591981, cornsilk: 16775388, crimson: 14423100, cyan: 65535, darkblue: 139, darkcyan: 35723, darkgoldenrod: 12092939, darkgray: 11119017, darkgreen: 25600, darkgrey: 11119017, darkkhaki: 12433259, darkmagenta: 9109643, darkolivegreen: 5597999, darkorange: 16747520, darkorchid: 10040012, darkred: 9109504, darksalmon: 15308410, darkseagreen: 9419919, darkslateblue: 4734347, darkslategray: 3100495, darkslategrey: 3100495, darkturquoise: 52945, darkviolet: 9699539, deeppink: 16716947, deepskyblue: 49151, dimgray: 6908265, dimgrey: 6908265, dodgerblue: 2003199, firebrick: 11674146, floralwhite: 16775920, forestgreen: 2263842, fuchsia: 16711935, gainsboro: 14474460, ghostwhite: 16316671, gold: 16766720, goldenrod: 14329120, gray: 8421504, green: 32768, greenyellow: 11403055, grey: 8421504, honeydew: 15794160, hotpink: 16738740, indianred: 13458524, indigo: 4915330, ivory: 16777200, khaki: 15787660, lavender: 15132410, lavenderblush: 16773365, lawngreen: 8190976, lemonchiffon: 16775885, lightblue: 11393254, lightcoral: 15761536, lightcyan: 14745599, lightgoldenrodyellow: 16448210, lightgray: 13882323, lightgreen: 9498256, lightgrey: 13882323, lightpink: 16758465, lightsalmon: 16752762, lightseagreen: 2142890, lightskyblue: 8900346, lightslategray: 7833753, lightslategrey: 7833753, lightsteelblue: 11584734, lightyellow: 16777184, lime: 65280, limegreen: 3329330, linen: 16445670, magenta: 16711935, maroon: 8388608, mediumaquamarine: 6737322, mediumblue: 205, mediumorchid: 12211667, mediumpurple: 9662683, mediumseagreen: 3978097, mediumslateblue: 8087790, mediumspringgreen: 64154, mediumturquoise: 4772300, mediumvioletred: 13047173, midnightblue: 1644912, mintcream: 16121850, mistyrose: 16770273, moccasin: 16770229, navajowhite: 16768685, navy: 128, oldlace: 16643558, olive: 8421376, olivedrab: 7048739, orange: 16753920, orangered: 16729344, orchid: 14315734, palegoldenrod: 15657130, palegreen: 10025880, paleturquoise: 11529966, palevioletred: 14381203, papayawhip: 16773077, peachpuff: 16767673, peru: 13468991, pink: 16761035, plum: 14524637, powderblue: 11591910, purple: 8388736, rebeccapurple: 6697881, red: 16711680, rosybrown: 12357519, royalblue: 4286945, saddlebrown: 9127187, salmon: 16416882, sandybrown: 16032864, seagreen: 3050327, seashell: 16774638, sienna: 10506797, silver: 12632256, skyblue: 8900331, slateblue: 6970061, slategray: 7372944, slategrey: 7372944, snow: 16775930, springgreen: 65407, steelblue: 4620980, tan: 13808780, teal: 32896, thistle: 14204888, tomato: 16737095, turquoise: 4251856, violet: 15631086, wheat: 16113331, white: 16777215, whitesmoke: 16119285, yellow: 16776960, yellowgreen: 10145074 };function Tt(t) {
    var n;return t = (t + "").trim().toLowerCase(), (n = vt.exec(t)) ? new Ut((n = parseInt(n[1], 16)) >> 8 & 15 | n >> 4 & 240, n >> 4 & 15 | 240 & n, (15 & n) << 4 | 15 & n, 1) : (n = yt.exec(t)) ? Nt(parseInt(n[1], 16)) : (n = gt.exec(t)) ? new Ut(n[1], n[2], n[3], 1) : (n = bt.exec(t)) ? new Ut(255 * n[1] / 100, 255 * n[2] / 100, 255 * n[3] / 100, 1) : (n = mt.exec(t)) ? Ct(n[1], n[2], n[3], n[4]) : (n = _t.exec(t)) ? Ct(255 * n[1] / 100, 255 * n[2] / 100, 255 * n[3] / 100, n[4]) : (n = wt.exec(t)) ? Dt(n[1], n[2] / 100, n[3] / 100, 1) : (n = xt.exec(t)) ? Dt(n[1], n[2] / 100, n[3] / 100, n[4]) : Mt.hasOwnProperty(t) ? Nt(Mt[t]) : "transparent" === t ? new Ut(NaN, NaN, NaN, 0) : null;
  }function Nt(t) {
    return new Ut(t >> 16 & 255, t >> 8 & 255, 255 & t, 1);
  }function Ct(t, n, e, r) {
    return r <= 0 && (t = n = e = NaN), new Ut(t, n, e, r);
  }function kt(t) {
    return t instanceof st || (t = Tt(t)), t ? new Ut((t = t.rgb()).r, t.g, t.b, t.opacity) : new Ut();
  }function At(t, n, e, r) {
    return 1 === arguments.length ? kt(t) : new Ut(t, n, e, null == r ? 1 : r);
  }function Ut(t, n, e, r) {
    this.r = +t, this.g = +n, this.b = +e, this.opacity = +r;
  }function Dt(t, n, e, r) {
    return r <= 0 ? t = n = e = NaN : e <= 0 || 1 <= e ? t = n = NaN : n <= 0 && (t = NaN), new St(t, n, e, r);
  }function St(t, n, e, r) {
    this.h = +t, this.s = +n, this.l = +e, this.opacity = +r;
  }function zt(t, n, e) {
    return 255 * (t < 60 ? n + (e - n) * t / 60 : t < 180 ? e : t < 240 ? n + (e - n) * (240 - t) / 60 : n);
  }ct(st, Tt, { displayable: function displayable() {
      return this.rgb().displayable();
    }, toString: function toString() {
      return this.rgb() + "";
    } }), ct(Ut, At, ft(st, { brighter: function brighter(t) {
      return t = null == t ? ht : Math.pow(ht, t), new Ut(this.r * t, this.g * t, this.b * t, this.opacity);
    }, darker: function darker(t) {
      return t = null == t ? .7 : Math.pow(.7, t), new Ut(this.r * t, this.g * t, this.b * t, this.opacity);
    }, rgb: function rgb() {
      return this;
    }, displayable: function displayable() {
      return 0 <= this.r && this.r <= 255 && 0 <= this.g && this.g <= 255 && 0 <= this.b && this.b <= 255 && 0 <= this.opacity && this.opacity <= 1;
    }, toString: function toString() {
      var t = this.opacity;return (1 === (t = isNaN(t) ? 1 : Math.max(0, Math.min(1, t))) ? "rgb(" : "rgba(") + Math.max(0, Math.min(255, Math.round(this.r) || 0)) + ", " + Math.max(0, Math.min(255, Math.round(this.g) || 0)) + ", " + Math.max(0, Math.min(255, Math.round(this.b) || 0)) + (1 === t ? ")" : ", " + t + ")");
    } })), ct(St, function (t, n, e, r) {
    return 1 === arguments.length ? function (t) {
      if (t instanceof St) return new St(t.h, t.s, t.l, t.opacity);if (t instanceof st || (t = Tt(t)), !t) return new St();if (t instanceof St) return t;var n = (t = t.rgb()).r / 255,
          e = t.g / 255,
          r = t.b / 255,
          i = Math.min(n, e, r),
          o = Math.max(n, e, r),
          a = NaN,
          u = o - i,
          c = (o + i) / 2;return u ? (a = n === o ? (e - r) / u + 6 * (e < r) : e === o ? (r - n) / u + 2 : (n - e) / u + 4, u /= c < .5 ? o + i : 2 - o - i, a *= 60) : u = 0 < c && c < 1 ? 0 : a, new St(a, u, c, t.opacity);
    }(t) : new St(t, n, e, null == r ? 1 : r);
  }, ft(st, { brighter: function brighter(t) {
      return t = null == t ? ht : Math.pow(ht, t), new St(this.h, this.s, this.l * t, this.opacity);
    }, darker: function darker(t) {
      return t = null == t ? .7 : Math.pow(.7, t), new St(this.h, this.s, this.l * t, this.opacity);
    }, rgb: function rgb() {
      var t = this.h % 360 + 360 * (this.h < 0),
          n = isNaN(t) || isNaN(this.s) ? 0 : this.s,
          e = this.l,
          r = e + (e < .5 ? e : 1 - e) * n,
          i = 2 * e - r;return new Ut(zt(240 <= t ? t - 240 : t + 120, i, r), zt(t, i, r), zt(t < 120 ? t + 240 : t - 120, i, r), this.opacity);
    }, displayable: function displayable() {
      return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && 0 <= this.l && this.l <= 1 && 0 <= this.opacity && this.opacity <= 1;
    } }));var Et = Math.PI / 180,
      Yt = 180 / Math.PI,
      Ft = .95047,
      qt = 1,
      Ht = 1.08883,
      Pt = 4 / 29,
      Xt = 6 / 29,
      Lt = 3 * Xt * Xt,
      jt = Xt * Xt * Xt;function Ot(t) {
    if (t instanceof It) return new It(t.l, t.a, t.b, t.opacity);if (t instanceof Zt) {
      var n = t.h * Et;return new It(t.l, Math.cos(n) * t.c, Math.sin(n) * t.c, t.opacity);
    }t instanceof Ut || (t = kt(t));var e = $t(t.r),
        r = $t(t.g),
        i = $t(t.b),
        o = Vt((.4124564 * e + .3575761 * r + .1804375 * i) / Ft),
        a = Vt((.2126729 * e + .7151522 * r + .072175 * i) / qt);return new It(116 * a - 16, 500 * (o - a), 200 * (a - Vt((.0193339 * e + .119192 * r + .9503041 * i) / Ht)), t.opacity);
  }function It(t, n, e, r) {
    this.l = +t, this.a = +n, this.b = +e, this.opacity = +r;
  }function Vt(t) {
    return jt < t ? Math.pow(t, 1 / 3) : t / Lt + Pt;
  }function Rt(t) {
    return Xt < t ? t * t * t : Lt * (t - Pt);
  }function Bt(t) {
    return 255 * (t <= .0031308 ? 12.92 * t : 1.055 * Math.pow(t, 1 / 2.4) - .055);
  }function $t(t) {
    return (t /= 255) <= .04045 ? t / 12.92 : Math.pow((t + .055) / 1.055, 2.4);
  }function Zt(t, n, e, r) {
    this.h = +t, this.c = +n, this.l = +e, this.opacity = +r;
  }ct(It, function (t, n, e, r) {
    return 1 === arguments.length ? Ot(t) : new It(t, n, e, null == r ? 1 : r);
  }, ft(st, { brighter: function brighter(t) {
      return new It(this.l + 18 * (null == t ? 1 : t), this.a, this.b, this.opacity);
    }, darker: function darker(t) {
      return new It(this.l - 18 * (null == t ? 1 : t), this.a, this.b, this.opacity);
    }, rgb: function rgb() {
      var t = (this.l + 16) / 116,
          n = isNaN(this.a) ? t : t + this.a / 500,
          e = isNaN(this.b) ? t : t - this.b / 200;return t = qt * Rt(t), new Ut(Bt(3.2404542 * (n = Ft * Rt(n)) - 1.5371385 * t - .4985314 * (e = Ht * Rt(e))), Bt(-.969266 * n + 1.8760108 * t + .041556 * e), Bt(.0556434 * n - .2040259 * t + 1.0572252 * e), this.opacity);
    } })), ct(Zt, function (t, n, e, r) {
    return 1 === arguments.length ? function (t) {
      if (t instanceof Zt) return new Zt(t.h, t.c, t.l, t.opacity);t instanceof It || (t = Ot(t));var n = Math.atan2(t.b, t.a) * Yt;return new Zt(n < 0 ? n + 360 : n, Math.sqrt(t.a * t.a + t.b * t.b), t.l, t.opacity);
    }(t) : new Zt(t, n, e, null == r ? 1 : r);
  }, ft(st, { brighter: function brighter(t) {
      return new Zt(this.h, this.c, this.l + 18 * (null == t ? 1 : t), this.opacity);
    }, darker: function darker(t) {
      return new Zt(this.h, this.c, this.l - 18 * (null == t ? 1 : t), this.opacity);
    }, rgb: function rgb() {
      return Ot(this).rgb();
    } }));var Wt = 1.78277,
      Qt = -.29227,
      Jt = -.90649,
      Gt = 1.97294,
      Kt = Gt * Jt,
      tn = Gt * Wt,
      nn = Wt * Qt - -.14861 * Jt;function en(t, n, e, r) {
    return 1 === arguments.length ? function (t) {
      if (t instanceof rn) return new rn(t.h, t.s, t.l, t.opacity);t instanceof Ut || (t = kt(t));var n = t.r / 255,
          e = t.g / 255,
          r = t.b / 255,
          i = (nn * r + Kt * n - tn * e) / (nn + Kt - tn),
          o = r - i,
          a = (Gt * (e - i) - Qt * o) / Jt,
          u = Math.sqrt(a * a + o * o) / (Gt * i * (1 - i)),
          c = u ? Math.atan2(a, o) * Yt - 120 : NaN;return new rn(c < 0 ? c + 360 : c, u, i, t.opacity);
    }(t) : new rn(t, n, e, null == r ? 1 : r);
  }function rn(t, n, e, r) {
    this.h = +t, this.s = +n, this.l = +e, this.opacity = +r;
  }ct(rn, en, ft(st, { brighter: function brighter(t) {
      return t = null == t ? ht : Math.pow(ht, t), new rn(this.h, this.s, this.l * t, this.opacity);
    }, darker: function darker(t) {
      return t = null == t ? .7 : Math.pow(.7, t), new rn(this.h, this.s, this.l * t, this.opacity);
    }, rgb: function rgb() {
      var t = isNaN(this.h) ? 0 : (this.h + 120) * Et,
          n = +this.l,
          e = isNaN(this.s) ? 0 : this.s * n * (1 - n),
          r = Math.cos(t),
          i = Math.sin(t);return new Ut(255 * (n + e * (-.14861 * r + Wt * i)), 255 * (n + e * (Qt * r + Jt * i)), 255 * (n + e * (Gt * r)), this.opacity);
    } }));var on = function on(t) {
    return function () {
      return t;
    };
  };function an(n, e) {
    return function (t) {
      return n + t * e;
    };
  }function un(o) {
    return 1 == (o = +o) ? cn : function (t, n) {
      return n - t ? (e = t, r = n, i = o, e = Math.pow(e, i), r = Math.pow(r, i) - e, i = 1 / i, function (t) {
        return Math.pow(e + t * r, i);
      }) : on(isNaN(t) ? n : t);var e, r, i;
    };
  }function cn(t, n) {
    var e = n - t;return e ? an(t, e) : on(isNaN(t) ? n : t);
  }var fn = function t(n) {
    var a = un(n);function e(n, t) {
      var e = a((n = At(n)).r, (t = At(t)).r),
          r = a(n.g, t.g),
          i = a(n.b, t.b),
          o = cn(n.opacity, t.opacity);return function (t) {
        return n.r = e(t), n.g = r(t), n.b = i(t), n.opacity = o(t), n + "";
      };
    }return e.gamma = t, e;
  }(1),
      sn = function sn(n, e) {
    return e -= n = +n, function (t) {
      return n + e * t;
    };
  },
      hn = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g,
      ln = new RegExp(hn.source, "g");var dn,
      pn,
      vn,
      yn,
      gn = 180 / Math.PI,
      bn = { translateX: 0, translateY: 0, rotate: 0, skewX: 0, scaleX: 1, scaleY: 1 },
      mn = function mn(t, n, e, r, i, o) {
    var a, u, c;return (a = Math.sqrt(t * t + n * n)) && (t /= a, n /= a), (c = t * e + n * r) && (e -= t * c, r -= n * c), (u = Math.sqrt(e * e + r * r)) && (e /= u, r /= u, c /= u), t * r < n * e && (t = -t, n = -n, c = -c, a = -a), { translateX: i, translateY: o, rotate: Math.atan2(n, t) * gn, skewX: Math.atan(c) * gn, scaleX: a, scaleY: u };
  };function _n(l, d, p, v) {
    function y(t) {
      return t.length ? t.pop() + " " : "";
    }return function (t, n) {
      var e,
          r,
          i,
          o,
          a,
          u,
          c,
          f,
          s = [],
          h = [];return t = l(t), n = l(n), function (t, n, e, r, i, o) {
        if (t !== e || n !== r) {
          var a = i.push("translate(", null, d, null, p);o.push({ i: a - 4, x: sn(t, e) }, { i: a - 2, x: sn(n, r) });
        } else (e || r) && i.push("translate(" + e + d + r + p);
      }(t.translateX, t.translateY, n.translateX, n.translateY, s, h), e = t.rotate, r = n.rotate, i = s, o = h, e !== r ? (180 < e - r ? r += 360 : 180 < r - e && (e += 360), o.push({ i: i.push(y(i) + "rotate(", null, v) - 2, x: sn(e, r) })) : r && i.push(y(i) + "rotate(" + r + v), a = t.skewX, u = n.skewX, c = s, f = h, a !== u ? f.push({ i: c.push(y(c) + "skewX(", null, v) - 2, x: sn(a, u) }) : u && c.push(y(c) + "skewX(" + u + v), function (t, n, e, r, i, o) {
        if (t !== e || n !== r) {
          var a = i.push(y(i) + "scale(", null, ",", null, ")");o.push({ i: a - 4, x: sn(t, e) }, { i: a - 2, x: sn(n, r) });
        } else 1 === e && 1 === r || i.push(y(i) + "scale(" + e + "," + r + ")");
      }(t.scaleX, t.scaleY, n.scaleX, n.scaleY, s, h), t = n = null, function (t) {
        for (var n, e = -1, r = h.length; ++e < r;) {
          s[(n = h[e]).i] = n.x(t);
        }return s.join("");
      };
    };
  }var wn = _n(function (t) {
    return "none" === t ? bn : (dn || (dn = document.createElement("DIV"), pn = document.documentElement, vn = document.defaultView), dn.style.transform = t, t = vn.getComputedStyle(pn.appendChild(dn), null).getPropertyValue("transform"), pn.removeChild(dn), t = t.slice(7, -1).split(","), mn(+t[0], +t[1], +t[2], +t[3], +t[4], +t[5]));
  }, "px, ", "px)", "deg)"),
      xn = _n(function (t) {
    return null == t ? bn : (yn || (yn = document.createElementNS("http://www.w3.org/2000/svg", "g")), yn.setAttribute("transform", t), (t = yn.transform.baseVal.consolidate()) ? (t = t.matrix, mn(t.a, t.b, t.c, t.d, t.e, t.f)) : bn);
  }, ", ", ")", ")"),
      Mn = Math.SQRT2;function Tn(t) {
    return ((t = Math.exp(t)) + 1 / t) / 2;
  }var Nn = function Nn(t, n) {
    var e,
        a,
        u = t[0],
        c = t[1],
        f = t[2],
        r = n[0],
        i = n[1],
        o = n[2],
        s = r - u,
        h = i - c,
        l = s * s + h * h;if (l < 1e-12) a = Math.log(o / f) / Mn, e = function e(t) {
      return [u + t * s, c + t * h, f * Math.exp(Mn * t * a)];
    };else {
      var d = Math.sqrt(l),
          p = (o * o - f * f + 4 * l) / (2 * f * 2 * d),
          v = (o * o - f * f - 4 * l) / (2 * o * 2 * d),
          y = Math.log(Math.sqrt(p * p + 1) - p),
          g = Math.log(Math.sqrt(v * v + 1) - v);a = (g - y) / Mn, e = function e(t) {
        var n,
            e,
            r = t * a,
            i = Tn(y),
            o = f / (2 * d) * (i * (e = Mn * r + y, ((e = Math.exp(2 * e)) - 1) / (e + 1)) - (n = y, ((n = Math.exp(n)) - 1 / n) / 2));return [u + o * s, c + o * h, f * i / Tn(Mn * r + y)];
      };
    }return e.duration = 1e3 * a, e;
  };function Cn(u) {
    return function t(a) {
      function n(n, t) {
        var e = u((n = en(n)).h, (t = en(t)).h),
            r = cn(n.s, t.s),
            i = cn(n.l, t.l),
            o = cn(n.opacity, t.opacity);return function (t) {
          return n.h = e(t), n.s = r(t), n.l = i(Math.pow(t, a)), n.opacity = o(t), n + "";
        };
      }return a = +a, n.gamma = t, n;
    }(1);
  }Cn(function (t, n) {
    var e = n - t;return e ? an(t, 180 < e || e < -180 ? e - 360 * Math.round(e / 360) : e) : on(isNaN(t) ? n : t);
  });var kn,
      An,
      Un = Cn(cn),
      Dn = 0,
      Sn = 0,
      zn = 0,
      En = 1e3,
      Yn = 0,
      Fn = 0,
      qn = 0,
      Hn = "object" == (typeof performance === "undefined" ? "undefined" : _typeof(performance)) && performance.now ? performance : Date,
      Pn = "object" == (typeof window === "undefined" ? "undefined" : _typeof(window)) && window.requestAnimationFrame ? window.requestAnimationFrame.bind(window) : function (t) {
    setTimeout(t, 17);
  };function Xn() {
    return Fn || (Pn(Ln), Fn = Hn.now() + qn);
  }function Ln() {
    Fn = 0;
  }function jn() {
    this._call = this._time = this._next = null;
  }function On(t, n, e) {
    var r = new jn();return r.restart(t, n, e), r;
  }function In() {
    Fn = (Yn = Hn.now()) + qn, Dn = Sn = 0;try {
      !function () {
        Xn(), ++Dn;for (var t, n = kn; n;) {
          0 <= (t = Fn - n._time) && n._call.call(null, t), n = n._next;
        }--Dn;
      }();
    } finally {
      Dn = 0, function () {
        var t,
            n,
            e = kn,
            r = 1 / 0;for (; e;) {
          e._call ? (r > e._time && (r = e._time), e = (t = e)._next) : (n = e._next, e._next = null, e = t ? t._next = n : kn = n);
        }An = t, Rn(r);
      }(), Fn = 0;
    }
  }function Vn() {
    var t = Hn.now(),
        n = t - Yn;En < n && (qn -= n, Yn = t);
  }function Rn(t) {
    Dn || (Sn && (Sn = clearTimeout(Sn)), 24 < t - Fn ? (t < 1 / 0 && (Sn = setTimeout(In, t - Hn.now() - qn)), zn && (zn = clearInterval(zn))) : (zn || (Yn = Hn.now(), zn = setInterval(Vn, En)), Dn = 1, Pn(In)));
  }jn.prototype = On.prototype = { constructor: jn, restart: function restart(t, n, e) {
      if ("function" != typeof t) throw new TypeError("callback is not a function");e = (null == e ? Xn() : +e) + (null == n ? 0 : +n), this._next || An === this || (An ? An._next = this : kn = this, An = this), this._call = t, this._time = e, Rn();
    }, stop: function stop() {
      this._call && (this._call = null, this._time = 1 / 0, Rn());
    } };var Bn = function Bn(n, e, t) {
    var r = new jn();return e = null == e ? 0 : +e, r.restart(function (t) {
      r.stop(), n(t + e);
    }, e, t), r;
  },
      $n = S("start", "end", "interrupt"),
      Zn = [],
      Wn = 0,
      Qn = 2,
      Jn = function Jn(t, n, e, r, i, o) {
    var a = t.__transition;if (a) {
      if (e in a) return;
    } else t.__transition = {};!function (o, a, u) {
      var c,
          f = o.__transition;function s(t) {
        var n, e, r, i;if (1 !== u.state) return l();for (n in f) {
          if ((i = f[n]).name === u.name) {
            if (3 === i.state) return Bn(s);4 === i.state ? (i.state = 6, i.timer.stop(), i.on.call("interrupt", o, o.__data__, i.index, i.group), delete f[n]) : +n < a && (i.state = 6, i.timer.stop(), delete f[n]);
          }
        }if (Bn(function () {
          3 === u.state && (u.state = 4, u.timer.restart(h, u.delay, u.time), h(t));
        }), u.state = Qn, u.on.call("start", o, o.__data__, u.index, u.group), u.state === Qn) {
          for (u.state = 3, c = new Array(r = u.tween.length), n = 0, e = -1; n < r; ++n) {
            (i = u.tween[n].value.call(o, o.__data__, u.index, u.group)) && (c[++e] = i);
          }c.length = e + 1;
        }
      }function h(t) {
        for (var n = t < u.duration ? u.ease.call(null, t / u.duration) : (u.timer.restart(l), u.state = 5, 1), e = -1, r = c.length; ++e < r;) {
          c[e].call(null, n);
        }5 === u.state && (u.on.call("end", o, o.__data__, u.index, u.group), l());
      }function l() {
        for (var t in u.state = 6, u.timer.stop(), delete f[a], f) {
          return;
        }delete o.__transition;
      }(f[a] = u).timer = On(function (t) {
        u.state = 1, u.timer.restart(s, u.delay, u.time), u.delay <= t && s(t - u.delay);
      }, 0, u.time);
    }(t, e, { name: n, index: r, group: i, on: $n, tween: Zn, time: o.time, delay: o.delay, duration: o.duration, ease: o.ease, timer: null, state: Wn });
  };function Gn(t, n) {
    var e = te(t, n);if (e.state > Wn) throw new Error("too late; already scheduled");return e;
  }function Kn(t, n) {
    var e = te(t, n);if (e.state > Qn) throw new Error("too late; already started");return e;
  }function te(t, n) {
    var e = t.__transition;if (!e || !(e = e[n])) throw new Error("transition not found");return e;
  }var ne = function ne(t, n) {
    var e,
        r,
        i,
        o = t.__transition,
        a = !0;if (o) {
      for (i in n = null == n ? null : n + "", o) {
        (e = o[i]).name === n ? (r = e.state > Qn && e.state < 5, e.state = 6, e.timer.stop(), r && e.on.call("interrupt", t, t.__data__, e.index, e.group), delete o[i]) : a = !1;
      }a && delete t.__transition;
    }
  };function ee(t, n, e) {
    var r = t._id;return t.each(function () {
      var t = Kn(this, r);(t.value || (t.value = {}))[n] = e.apply(this, arguments);
    }), function (t) {
      return te(t, r).value[n];
    };
  }var re = function re(t, n) {
    var e;return ("number" == typeof n ? sn : n instanceof Tt ? fn : (e = Tt(n)) ? (n = e, fn) : function (t, r) {
      var n,
          e,
          i,
          o,
          a,
          u = hn.lastIndex = ln.lastIndex = 0,
          c = -1,
          f = [],
          s = [];for (t += "", r += ""; (n = hn.exec(t)) && (e = ln.exec(r));) {
        (i = e.index) > u && (i = r.slice(u, i), f[c] ? f[c] += i : f[++c] = i), (n = n[0]) === (e = e[0]) ? f[c] ? f[c] += e : f[++c] = e : (f[++c] = null, s.push({ i: c, x: sn(n, e) })), u = ln.lastIndex;
      }return u < r.length && (i = r.slice(u), f[c] ? f[c] += i : f[++c] = i), f.length < 2 ? s[0] ? (a = s[0].x, function (t) {
        return a(t) + "";
      }) : (o = r, function () {
        return o;
      }) : (r = s.length, function (t) {
        for (var n, e = 0; e < r; ++e) {
          f[(n = s[e]).i] = n.x(t);
        }return f.join("");
      });
    })(t, n);
  };var ie = $.prototype.constructor;var oe = 0;function ae(t, n, e, r) {
    this._groups = t, this._parents = n, this._name = e, this._id = r;
  }function ue() {
    return ++oe;
  }var ce = $.prototype;ae.prototype = function (t) {
    return $().transition(t);
  }.prototype = { constructor: ae, select: function select(t) {
      var n = this._name,
          e = this._id;"function" != typeof t && (t = d(t));for (var r = this._groups, i = r.length, o = new Array(i), a = 0; a < i; ++a) {
        for (var u, c, f = r[a], s = f.length, h = o[a] = new Array(s), l = 0; l < s; ++l) {
          (u = f[l]) && (c = t.call(u, u.__data__, l, f)) && ("__data__" in u && (c.__data__ = u.__data__), h[l] = c, Jn(h[l], n, e, l, h, te(u, e)));
        }
      }return new ae(o, this._parents, n, e);
    }, selectAll: function selectAll(t) {
      var n = this._name,
          e = this._id;"function" != typeof t && (t = g(t));for (var r = this._groups, i = r.length, o = [], a = [], u = 0; u < i; ++u) {
        for (var c, f = r[u], s = f.length, h = 0; h < s; ++h) {
          if (c = f[h]) {
            for (var l, d = t.call(c, c.__data__, h, f), p = te(c, e), v = 0, y = d.length; v < y; ++v) {
              (l = d[v]) && Jn(l, n, e, v, d, p);
            }o.push(d), a.push(c);
          }
        }
      }return new ae(o, a, n, e);
    }, filter: function filter(t) {
      "function" != typeof t && (t = p(t));for (var n = this._groups, e = n.length, r = new Array(e), i = 0; i < e; ++i) {
        for (var o, a = n[i], u = a.length, c = r[i] = [], f = 0; f < u; ++f) {
          (o = a[f]) && t.call(o, o.__data__, f, a) && c.push(o);
        }
      }return new ae(r, this._parents, this._name, this._id);
    }, merge: function merge(t) {
      if (t._id !== this._id) throw new Error();for (var n = this._groups, e = t._groups, r = n.length, i = e.length, o = Math.min(r, i), a = new Array(r), u = 0; u < o; ++u) {
        for (var c, f = n[u], s = e[u], h = f.length, l = a[u] = new Array(h), d = 0; d < h; ++d) {
          (c = f[d] || s[d]) && (l[d] = c);
        }
      }for (; u < r; ++u) {
        a[u] = n[u];
      }return new ae(a, this._parents, this._name, this._id);
    }, selection: function selection() {
      return new ie(this._groups, this._parents);
    }, transition: function transition() {
      for (var t = this._name, n = this._id, e = ue(), r = this._groups, i = r.length, o = 0; o < i; ++o) {
        for (var a, u = r[o], c = u.length, f = 0; f < c; ++f) {
          if (a = u[f]) {
            var s = te(a, n);Jn(a, t, e, f, u, { time: s.time + s.delay + s.duration, delay: 0, duration: s.duration, ease: s.ease });
          }
        }
      }return new ae(r, this._parents, t, e);
    }, call: ce.call, nodes: ce.nodes, node: ce.node, size: ce.size, empty: ce.empty, each: ce.each, on: function on(t, n) {
      var e,
          r,
          i,
          o,
          a,
          u,
          c = this._id;return arguments.length < 2 ? te(this.node(), c).on.on(t) : this.each((e = c, i = n, u = ((r = t) + "").trim().split(/^|\s+/).every(function (t) {
        var n = t.indexOf(".");return 0 <= n && (t = t.slice(0, n)), !t || "start" === t;
      }) ? Gn : Kn, function () {
        var t = u(this, e),
            n = t.on;n !== o && (a = (o = n).copy()).on(r, i), t.on = a;
      }));
    }, attr: function attr(t, n) {
      var e = u(t),
          r = "transform" === e ? xn : re;return this.attrTween(t, "function" == typeof n ? (e.local ? function (e, r, i) {
        var o, a, u;return function () {
          var t,
              n = i(this);if (null != n) return (t = this.getAttributeNS(e.space, e.local)) === n ? null : t === o && n === a ? u : u = r(o = t, a = n);this.removeAttributeNS(e.space, e.local);
        };
      } : function (e, r, i) {
        var o, a, u;return function () {
          var t,
              n = i(this);if (null != n) return (t = this.getAttribute(e)) === n ? null : t === o && n === a ? u : u = r(o = t, a = n);this.removeAttribute(e);
        };
      })(e, r, ee(this, "attr." + t, n)) : null == n ? (e.local ? function (t) {
        return function () {
          this.removeAttributeNS(t.space, t.local);
        };
      } : function (t) {
        return function () {
          this.removeAttribute(t);
        };
      })(e) : (e.local ? function (n, e, r) {
        var i, o;return function () {
          var t = this.getAttributeNS(n.space, n.local);return t === r ? null : t === i ? o : o = e(i = t, r);
        };
      } : function (n, e, r) {
        var i, o;return function () {
          var t = this.getAttribute(n);return t === r ? null : t === i ? o : o = e(i = t, r);
        };
      })(e, r, n + ""));
    }, attrTween: function attrTween(t, n) {
      var e = "attr." + t;if (arguments.length < 2) return (e = this.tween(e)) && e._value;if (null == n) return this.tween(e, null);if ("function" != typeof n) throw new Error();var r = u(t);return this.tween(e, (r.local ? function (r, t) {
        function n() {
          var n = this,
              e = t.apply(n, arguments);return e && function (t) {
            n.setAttributeNS(r.space, r.local, e(t));
          };
        }return n._value = t, n;
      } : function (r, t) {
        function n() {
          var n = this,
              e = t.apply(n, arguments);return e && function (t) {
            n.setAttribute(r, e(t));
          };
        }return n._value = t, n;
      })(r, n));
    }, style: function (_style) {
      function style(_x, _x2, _x3) {
        return _style.apply(this, arguments);
      }

      style.toString = function () {
        return _style.toString();
      };

      return style;
    }(function (t, n, e) {
      var r,
          i,
          o,
          a,
          u,
          c,
          f,
          s,
          h,
          l,
          d,
          p,
          v,
          y,
          g,
          b,
          m,
          _ = "transform" == (t += "") ? wn : re;return null == n ? this.styleTween(t, (v = t, y = _, function () {
        var t = w(this, v),
            n = (this.style.removeProperty(v), style(this, v));return t === n ? null : t === g && n === b ? m : m = y(g = t, b = n);
      })).on("end.style." + t, (p = t, function () {
        this.style.removeProperty(p);
      })) : this.styleTween(t, "function" == typeof n ? (f = _, s = ee(this, "style." + (c = t), n), function () {
        var t = w(this, c),
            n = s(this);return null == n && (this.style.removeProperty(c), n = style(this, c)), t === n ? null : t === h && n === l ? d : d = f(h = t, l = n);
      }) : (r = t, i = _, o = n + "", function () {
        var t = w(this, r);return t === o ? null : t === a ? u : u = i(a = t, o);
      }), e);
    }), styleTween: function styleTween(t, n, e) {
      var r = "style." + (t += "");if (arguments.length < 2) return (r = this.tween(r)) && r._value;if (null == n) return this.tween(r, null);if ("function" != typeof n) throw new Error();return this.tween(r, function (r, t, i) {
        function n() {
          var n = this,
              e = t.apply(n, arguments);return e && function (t) {
            n.style.setProperty(r, e(t), i);
          };
        }return n._value = t, n;
      }(t, n, null == e ? "" : e));
    }, text: function text(t) {
      return this.tween("text", "function" == typeof t ? (e = ee(this, "text", t), function () {
        var t = e(this);this.textContent = null == t ? "" : t;
      }) : (n = null == t ? "" : t + "", function () {
        this.textContent = n;
      }));var n, e;
    }, remove: function remove() {
      return this.on("end.remove", (e = this._id, function () {
        var t = this.parentNode;for (var n in this.__transition) {
          if (+n !== e) return;
        }t && t.removeChild(this);
      }));var e;
    }, tween: function tween(t, n) {
      var e = this._id;if (t += "", arguments.length < 2) {
        for (var r, i = te(this.node(), e).tween, o = 0, a = i.length; o < a; ++o) {
          if ((r = i[o]).name === t) return r.value;
        }return null;
      }return this.each((null == n ? function (i, o) {
        var a, u;return function () {
          var t = Kn(this, i),
              n = t.tween;if (n !== a) for (var e = 0, r = (u = a = n).length; e < r; ++e) {
            if (u[e].name === o) {
              (u = u.slice()).splice(e, 1);break;
            }
          }t.tween = u;
        };
      } : function (o, a, u) {
        var c, f;if ("function" != typeof u) throw new Error();return function () {
          var t = Kn(this, o),
              n = t.tween;if (n !== c) {
            f = (c = n).slice();for (var e = { name: a, value: u }, r = 0, i = f.length; r < i; ++r) {
              if (f[r].name === a) {
                f[r] = e;break;
              }
            }r === i && f.push(e);
          }t.tween = f;
        };
      })(e, t, n));
    }, delay: function delay(t) {
      var n = this._id;return arguments.length ? this.each(("function" == typeof t ? function (t, n) {
        return function () {
          Gn(this, t).delay = +n.apply(this, arguments);
        };
      } : function (t, n) {
        return n = +n, function () {
          Gn(this, t).delay = n;
        };
      })(n, t)) : te(this.node(), n).delay;
    }, duration: function duration(t) {
      var n = this._id;return arguments.length ? this.each(("function" == typeof t ? function (t, n) {
        return function () {
          Kn(this, t).duration = +n.apply(this, arguments);
        };
      } : function (t, n) {
        return n = +n, function () {
          f(this, t).duration = n;
        };
      })(n, t)) : te(this.node(), n).duration;
    }, ease: function ease(t) {
      var n = this._id;return arguments.length ? this.each(function (t, n) {
        if ("function" != typeof n) throw new Error();return function () {
          Kn(this, t).ease = n;
        };
      }(n, t)) : te(this.node(), n).ease;
    } };var fe = { time: null, delay: 0, duration: 250, ease: function ease(t) {
      return ((t *= 2) <= 1 ? t * t * t : (t -= 2) * t * t + 2) / 2;
    } };function se(t, n) {
    for (var e; !(e = t.__transition) || !(e = e[n]);) {
      if (!(t = t.parentNode)) return fe.time = Xn(), fe;
    }return e;
  }$.prototype.interrupt = function (t) {
    return this.each(function () {
      ne(this, t);
    });
  }, $.prototype.transition = function (t) {
    var n, e;t instanceof ae ? (n = t._id, t = t._name) : (n = ue(), (e = fe).time = Xn(), t = null == t ? null : t + "");for (var r = this._groups, i = r.length, o = 0; o < i; ++o) {
      for (var a, u = r[o], c = u.length, f = 0; f < c; ++f) {
        (a = u[f]) && Jn(a, t, n, f, u, e || se(a, n));
      }
    }return new ae(r, this._parents, t, n);
  };["e", "w"].map(he), ["n", "s"].map(he), ["n", "e", "s", "w", "nw", "ne", "se", "sw"].map(he);function he(t) {
    return { type: t };
  }var le = "$";function de() {}function pe(t, n) {
    var e = new de();if (t instanceof de) t.each(function (t, n) {
      e.set(n, t);
    });else if (Array.isArray(t)) {
      var r,
          i = -1,
          o = t.length;if (null == n) for (; ++i < o;) {
        e.set(i, t[i]);
      } else for (; ++i < o;) {
        e.set(n(r = t[i], i, t), r);
      }
    } else if (t) for (var a in t) {
      e.set(a, t[a]);
    }return e;
  }de.prototype = pe.prototype = { constructor: de, has: function has(t) {
      return le + t in this;
    }, get: function get(t) {
      return this[le + t];
    }, set: function set(t, n) {
      return this[le + t] = n, this;
    }, remove: function remove(t) {
      var n = le + t;return n in this && delete this[n];
    }, clear: function clear() {
      for (var t in this) {
        t[0] === le && delete this[t];
      }
    }, keys: function keys() {
      var t = [];for (var n in this) {
        n[0] === le && t.push(n.slice(1));
      }return t;
    }, values: function values() {
      var t = [];for (var n in this) {
        n[0] === le && t.push(this[n]);
      }return t;
    }, entries: function entries() {
      var t = [];for (var n in this) {
        n[0] === le && t.push({ key: n.slice(1), value: this[n] });
      }return t;
    }, size: function size() {
      var t = 0;for (var n in this) {
        n[0] === le && ++t;
      }return t;
    }, empty: function empty() {
      for (var t in this) {
        if (t[0] === le) return !1;
      }return !0;
    }, each: function each(t) {
      for (var n in this) {
        n[0] === le && t(this[n], n.slice(1), this);
      }
    } };var ve = {},
      ye = {};function ge(t) {
    return new Function("d", "return {" + t.map(function (t, n) {
      return JSON.stringify(t) + ": d[" + n + "]";
    }).join(",") + "}");
  }var be = function be(o) {
    var n = new RegExp('["' + o + "\n\r]"),
        h = o.charCodeAt(0);function e(r, t) {
      var n,
          e = [],
          i = r.length,
          o = 0,
          a = 0,
          u = i <= 0,
          c = !1;function f() {
        if (u) return ye;if (c) return c = !1, ve;var t,
            n,
            e = o;if (34 === r.charCodeAt(e)) {
          for (; o++ < i && 34 !== r.charCodeAt(o) || 34 === r.charCodeAt(++o);) {}return (t = o) >= i ? u = !0 : 10 === (n = r.charCodeAt(o++)) ? c = !0 : 13 === n && (c = !0, 10 === r.charCodeAt(o) && ++o), r.slice(e + 1, t - 1).replace(/""/g, '"');
        }for (; o < i;) {
          if (10 === (n = r.charCodeAt(t = o++))) c = !0;else if (13 === n) c = !0, 10 === r.charCodeAt(o) && ++o;else if (n !== h) continue;return r.slice(e, t);
        }return u = !0, r.slice(e, i);
      }for (10 === r.charCodeAt(i - 1) && --i, 13 === r.charCodeAt(i - 1) && --i; (n = f()) !== ye;) {
        for (var s = []; n !== ve && n !== ye;) {
          s.push(n), n = f();
        }t && null == (s = t(s, a++)) || e.push(s);
      }return e;
    }function r(t) {
      return t.map(a).join(o);
    }function a(t) {
      return null == t ? "" : n.test(t += "") ? '"' + t.replace(/"/g, '""') + '"' : t;
    }return { parse: function parse(t, o) {
        var a,
            u,
            n = e(t, function (t, n) {
          if (a) return a(t, n - 1);var e, r, i;u = t, a = o ? (r = o, i = ge(e = t), function (t, n) {
            return r(i(t), n, e);
          }) : ge(t);
        });return n.columns = u || [], n;
      }, parseRows: e, format: function format(t, e) {
        var n, r, i;return null == e && (n = t, r = Object.create(null), i = [], n.forEach(function (t) {
          for (var n in t) {
            n in r || i.push(r[n] = n);
          }
        }), e = i), [e.map(a).join(o)].concat(t.map(function (n) {
          return e.map(function (t) {
            return a(n[t]);
          }).join(o);
        })).join("\n");
      }, formatRows: function formatRows(t) {
        return t.map(r).join("\n");
      } };
  },
      me = (be(","), be("\t"), function (t) {
    return function () {
      return t;
    };
  }),
      _e = function _e() {
    return 1e-6 * (Math.random() - .5);
  };function we(t, n, e, r) {
    if (isNaN(n) || isNaN(e)) return t;var i,
        o,
        a,
        u,
        c,
        f,
        s,
        h,
        l,
        d = t._root,
        p = { data: r },
        v = t._x0,
        y = t._y0,
        g = t._x1,
        b = t._y1;if (!d) return t._root = p, t;for (; d.length;) {
      if ((f = n >= (o = (v + g) / 2)) ? v = o : g = o, (s = e >= (a = (y + b) / 2)) ? y = a : b = a, !(d = (i = d)[h = s << 1 | f])) return i[h] = p, t;
    }if (u = +t._x.call(null, d.data), c = +t._y.call(null, d.data), n === u && e === c) return p.next = d, i ? i[h] = p : t._root = p, t;for (; i = i ? i[h] = new Array(4) : t._root = new Array(4), (f = n >= (o = (v + g) / 2)) ? v = o : g = o, (s = e >= (a = (y + b) / 2)) ? y = a : b = a, (h = s << 1 | f) == (l = (a <= c) << 1 | o <= u);) {}return i[l] = d, i[h] = p, t;
  }var xe = function xe(t, n, e, r, i) {
    this.node = t, this.x0 = n, this.y0 = e, this.x1 = r, this.y1 = i;
  };function Me(t) {
    return t[0];
  }function Te(t) {
    return t[1];
  }function Ne(t, n, e) {
    var r = new Ce(null == n ? Me : n, null == e ? Te : e, NaN, NaN, NaN, NaN);return null == t ? r : r.addAll(t);
  }function Ce(t, n, e, r, i, o) {
    this._x = t, this._y = n, this._x0 = e, this._y0 = r, this._x1 = i, this._y1 = o, this._root = void 0;
  }function ke(t) {
    for (var n = { data: t.data }, e = n; t = t.next;) {
      e = e.next = { data: t.data };
    }return n;
  }var Ae = Ne.prototype = Ce.prototype;function Ue(t) {
    return t.x + t.vx;
  }function De(t) {
    return t.y + t.vy;
  }Ae.copy = function () {
    var t,
        n,
        e = new Ce(this._x, this._y, this._x0, this._y0, this._x1, this._y1),
        r = this._root;if (!r) return e;if (!r.length) return e._root = ke(r), e;for (t = [{ source: r, target: e._root = new Array(4) }]; r = t.pop();) {
      for (var i = 0; i < 4; ++i) {
        (n = r.source[i]) && (n.length ? t.push({ source: n, target: r.target[i] = new Array(4) }) : r.target[i] = ke(n));
      }
    }return e;
  }, Ae.add = function (t) {
    var n = +this._x.call(null, t),
        e = +this._y.call(null, t);return we(this.cover(n, e), n, e, t);
  }, Ae.addAll = function (t) {
    var n,
        e,
        r,
        i,
        o = t.length,
        a = new Array(o),
        u = new Array(o),
        c = 1 / 0,
        f = 1 / 0,
        s = -1 / 0,
        h = -1 / 0;for (e = 0; e < o; ++e) {
      isNaN(r = +this._x.call(null, n = t[e])) || isNaN(i = +this._y.call(null, n)) || ((a[e] = r) < c && (c = r), s < r && (s = r), (u[e] = i) < f && (f = i), h < i && (h = i));
    }for (s < c && (c = this._x0, s = this._x1), h < f && (f = this._y0, h = this._y1), this.cover(c, f).cover(s, h), e = 0; e < o; ++e) {
      we(this, a[e], u[e], t[e]);
    }return this;
  }, Ae.cover = function (t, n) {
    if (isNaN(t = +t) || isNaN(n = +n)) return this;var e = this._x0,
        r = this._y0,
        i = this._x1,
        o = this._y1;if (isNaN(e)) i = (e = Math.floor(t)) + 1, o = (r = Math.floor(n)) + 1;else {
      if (!(t < e || i < t || n < r || o < n)) return this;var a,
          u,
          c = i - e,
          f = this._root;switch (u = (n < (r + o) / 2) << 1 | t < (e + i) / 2) {case 0:
          for (; (a = new Array(4))[u] = f, f = a, o = r + (c *= 2), (i = e + c) < t || o < n;) {}break;case 1:
          for (; (a = new Array(4))[u] = f, f = a, o = r + (c *= 2), t < (e = i - c) || o < n;) {}break;case 2:
          for (; (a = new Array(4))[u] = f, f = a, r = o - (c *= 2), (i = e + c) < t || n < r;) {}break;case 3:
          for (; (a = new Array(4))[u] = f, f = a, r = o - (c *= 2), t < (e = i - c) || n < r;) {}}this._root && this._root.length && (this._root = f);
    }return this._x0 = e, this._y0 = r, this._x1 = i, this._y1 = o, this;
  }, Ae.data = function () {
    var n = [];return this.visit(function (t) {
      if (!t.length) for (; n.push(t.data), t = t.next;) {}
    }), n;
  }, Ae.extent = function (t) {
    return arguments.length ? this.cover(+t[0][0], +t[0][1]).cover(+t[1][0], +t[1][1]) : isNaN(this._x0) ? void 0 : [[this._x0, this._y0], [this._x1, this._y1]];
  }, Ae.find = function (t, n, e) {
    var r,
        i,
        o,
        a,
        u,
        c,
        f,
        s = this._x0,
        h = this._y0,
        l = this._x1,
        d = this._y1,
        p = [],
        v = this._root;for (v && p.push(new xe(v, s, h, l, d)), null == e ? e = 1 / 0 : (s = t - e, h = n - e, l = t + e, d = n + e, e *= e); c = p.pop();) {
      if (!(!(v = c.node) || (i = c.x0) > l || (o = c.y0) > d || (a = c.x1) < s || (u = c.y1) < h)) if (v.length) {
        var y = (i + a) / 2,
            g = (o + u) / 2;p.push(new xe(v[3], y, g, a, u), new xe(v[2], i, g, y, u), new xe(v[1], y, o, a, g), new xe(v[0], i, o, y, g)), (f = (g <= n) << 1 | y <= t) && (c = p[p.length - 1], p[p.length - 1] = p[p.length - 1 - f], p[p.length - 1 - f] = c);
      } else {
        var b = t - +this._x.call(null, v.data),
            m = n - +this._y.call(null, v.data),
            _ = b * b + m * m;if (_ < e) {
          var w = Math.sqrt(e = _);s = t - w, h = n - w, l = t + w, d = n + w, r = v.data;
        }
      }
    }return r;
  }, Ae.remove = function (t) {
    if (isNaN(o = +this._x.call(null, t)) || isNaN(a = +this._y.call(null, t))) return this;var n,
        e,
        r,
        i,
        o,
        a,
        u,
        c,
        f,
        s,
        h,
        l,
        d = this._root,
        p = this._x0,
        v = this._y0,
        y = this._x1,
        g = this._y1;if (!d) return this;if (d.length) for (;;) {
      if ((f = o >= (u = (p + y) / 2)) ? p = u : y = u, (s = a >= (c = (v + g) / 2)) ? v = c : g = c, !(d = (n = d)[h = s << 1 | f])) return this;if (!d.length) break;(n[h + 1 & 3] || n[h + 2 & 3] || n[h + 3 & 3]) && (e = n, l = h);
    }for (; d.data !== t;) {
      if (!(d = (r = d).next)) return this;
    }return (i = d.next) && delete d.next, r ? i ? r.next = i : delete r.next : n ? (i ? n[h] = i : delete n[h], (d = n[0] || n[1] || n[2] || n[3]) && d === (n[3] || n[2] || n[1] || n[0]) && !d.length && (e ? e[l] = d : this._root = d)) : this._root = i, this;
  }, Ae.removeAll = function (t) {
    for (var n = 0, e = t.length; n < e; ++n) {
      this.remove(t[n]);
    }return this;
  }, Ae.root = function () {
    return this._root;
  }, Ae.size = function () {
    var n = 0;return this.visit(function (t) {
      if (!t.length) for (; ++n, t = t.next;) {}
    }), n;
  }, Ae.visit = function (t) {
    var n,
        e,
        r,
        i,
        o,
        a,
        u = [],
        c = this._root;for (c && u.push(new xe(c, this._x0, this._y0, this._x1, this._y1)); n = u.pop();) {
      if (!t(c = n.node, r = n.x0, i = n.y0, o = n.x1, a = n.y1) && c.length) {
        var f = (r + o) / 2,
            s = (i + a) / 2;(e = c[3]) && u.push(new xe(e, f, s, o, a)), (e = c[2]) && u.push(new xe(e, r, s, f, a)), (e = c[1]) && u.push(new xe(e, f, i, o, s)), (e = c[0]) && u.push(new xe(e, r, i, f, s));
      }
    }return this;
  }, Ae.visitAfter = function (t) {
    var n,
        e = [],
        r = [];for (this._root && e.push(new xe(this._root, this._x0, this._y0, this._x1, this._y1)); n = e.pop();) {
      var i = n.node;if (i.length) {
        var o,
            a = n.x0,
            u = n.y0,
            c = n.x1,
            f = n.y1,
            s = (a + c) / 2,
            h = (u + f) / 2;(o = i[0]) && e.push(new xe(o, a, u, s, h)), (o = i[1]) && e.push(new xe(o, s, u, c, h)), (o = i[2]) && e.push(new xe(o, a, h, s, f)), (o = i[3]) && e.push(new xe(o, s, h, c, f));
      }r.push(n);
    }for (; n = r.pop();) {
      t(n.node, n.x0, n.y0, n.x1, n.y1);
    }return this;
  }, Ae.x = function (t) {
    return arguments.length ? (this._x = t, this) : this._x;
  }, Ae.y = function (t) {
    return arguments.length ? (this._y = t, this) : this._y;
  };function Se(t) {
    return t.index;
  }function ze(t, n) {
    var e = t.get(n);if (!e) throw new Error("missing: " + n);return e;
  }function Ee(t) {
    return t.x;
  }function Ye(t) {
    return t.y;
  }var Fe = Math.PI * (3 - Math.sqrt(5));var qe = function qe() {
    return new He();
  };function He() {
    this.reset();
  }He.prototype = { constructor: He, reset: function reset() {
      this.s = this.t = 0;
    }, add: function add(t) {
      Xe(Pe, t, this.t), Xe(this, Pe.s, this.s), this.s ? this.t += Pe.t : this.s = Pe.t;
    }, valueOf: function valueOf() {
      return this.s;
    } };var Pe = new He();function Xe(t, n, e) {
    var r = t.s = n + e,
        i = r - n,
        o = r - i;t.t = n - o + (e - i);
  }var Le = Math.PI,
      je = Le / 2,
      Oe = Math.atan,
      Ie = Math.atan2,
      Ve = Math.cos,
      Re = Math.sin,
      Be = Math.sqrt;function $e(t) {
    return 1 < t ? je : t < -1 ? -je : Math.asin(t);
  }qe(), qe(), qe(), qe(), qe(), qe(), qe(), qe();function Ze(o) {
    return function (t, n) {
      var e = Ve(t),
          r = Ve(n),
          i = o(e * r);return [i * r * Re(t), i * Re(n)];
    };
  }function We(a) {
    return function (t, n) {
      var e = Be(t * t + n * n),
          r = a(e),
          i = Re(r),
          o = Ve(r);return [Ie(t * i, e * o), $e(e && n * i / e)];
    };
  }Ze(function (t) {
    return Be(2 / (1 + t));
  }).invert = We(function (t) {
    return 2 * $e(t / 2);
  }), Ze(function (t) {
    return (t = 1 < (n = t) ? 0 : n < -1 ? Le : Math.acos(n)) && t / Re(t);var n;
  }).invert = We(function (t) {
    return t;
  }), We(Oe), We($e), We(function (t) {
    return 2 * Oe(t);
  });var Qe = new Date(),
      Je = new Date();function Ge(o, a, e, r) {
    function u(t) {
      return o(t = new Date(+t)), t;
    }return (u.floor = u).ceil = function (t) {
      return o(t = new Date(t - 1)), a(t, 1), o(t), t;
    }, u.round = function (t) {
      var n = u(t),
          e = u.ceil(t);return t - n < e - t ? n : e;
    }, u.offset = function (t, n) {
      return a(t = new Date(+t), null == n ? 1 : Math.floor(n)), t;
    }, u.range = function (t, n, e) {
      var r,
          i = [];if (t = u.ceil(t), e = null == e ? 1 : Math.floor(e), !(t < n && 0 < e)) return i;for (; i.push(r = new Date(+t)), a(t, e), o(t), r < t && t < n;) {}return i;
    }, u.filter = function (e) {
      return Ge(function (t) {
        if (t <= t) for (; o(t), !e(t);) {
          t.setTime(t - 1);
        }
      }, function (t, n) {
        if (t <= t) if (n < 0) for (; ++n <= 0;) {
          for (; a(t, -1), !e(t);) {}
        } else for (; 0 <= --n;) {
          for (; a(t, 1), !e(t);) {}
        }
      });
    }, e && (u.count = function (t, n) {
      return Pt.setTime(+t), Xt.setTime(+n), o(Pt), o(Xt), Math.floor(e(Qe, Je));
    }, u.every = function (n) {
      return n = Math.floor(n), isFinite(n) && 0 < n ? 1 < n ? u.filter(r ? function (t) {
        return r(t) % n == 0;
      } : function (t) {
        return u.count(0, t) % n == 0;
      }) : u : null;
    }), u;
  }var Ke = Ge(function () {}, function (t, n) {
    t.setTime(+t + n);
  }, function (t, n) {
    return n - t;
  });Ke.every = function (e) {
    return e = Math.floor(e), isFinite(e) && 0 < e ? 1 < e ? Ge(function (t) {
      t.setTime(Math.floor(t / e) * e);
    }, function (t, n) {
      t.setTime(+t + n * e);
    }, function (t, n) {
      return (n - t) / e;
    }) : Ke : null;
  };var tr = 6e4,
      nr = 36e5,
      er = (Ge(function (t) {
    t.setTime(1e3 * Math.floor(t / 1e3));
  }, function (t, n) {
    t.setTime(+t + 1e3 * n);
  }, function (t, n) {
    return (n - t) / 1e3;
  }, function (t) {
    return t.getUTCSeconds();
  }), Ge(function (t) {
    t.setTime(Math.floor(t / tr) * tr);
  }, function (t, n) {
    t.setTime(+t + n * tr);
  }, function (t, n) {
    return (n - t) / tr;
  }, function (t) {
    return t.getMinutes();
  }), Ge(function (t) {
    var n = t.getTimezoneOffset() * tr % nr;n < 0 && (n += nr), t.setTime(Math.floor((+t - n) / nr) * nr + n);
  }, function (t, n) {
    t.setTime(+t + n * nr);
  }, function (t, n) {
    return (n - t) / nr;
  }, function (t) {
    return t.getHours();
  }), Ge(function (t) {
    t.setHours(0, 0, 0, 0);
  }, function (t, n) {
    t.setDate(t.getDate() + n);
  }, function (t, n) {
    return (n - t - (n.getTimezoneOffset() - t.getTimezoneOffset()) * tr) / 864e5;
  }, function (t) {
    return t.getDate() - 1;
  }));function rr(n) {
    return Ge(function (t) {
      t.setDate(t.getDate() - (t.getDay() + 7 - n) % 7), t.setHours(0, 0, 0, 0);
    }, function (t, n) {
      t.setDate(t.getDate() + 7 * n);
    }, function (t, n) {
      return (n - t - (n.getTimezoneOffset() - t.getTimezoneOffset()) * tr) / 6048e5;
    });
  }var ir = rr(0),
      or = rr(1),
      ar = (rr(2), rr(3), rr(4)),
      ur = (rr(5), rr(6), Ge(function (t) {
    t.setDate(1), t.setHours(0, 0, 0, 0);
  }, function (t, n) {
    t.setMonth(t.getMonth() + n);
  }, function (t, n) {
    return n.getMonth() - t.getMonth() + 12 * (n.getFullYear() - t.getFullYear());
  }, function (t) {
    return t.getMonth();
  }), Ge(function (t) {
    t.setMonth(0, 1), t.setHours(0, 0, 0, 0);
  }, function (t, n) {
    t.setFullYear(t.getFullYear() + n);
  }, function (t, n) {
    return n.getFullYear() - t.getFullYear();
  }, function (t) {
    return t.getFullYear();
  }));ur.every = function (e) {
    return isFinite(e = Math.floor(e)) && 0 < e ? Ge(function (t) {
      t.setFullYear(Math.floor(t.getFullYear() / e) * e), t.setMonth(0, 1), t.setHours(0, 0, 0, 0);
    }, function (t, n) {
      t.setFullYear(t.getFullYear() + n * e);
    }) : null;
  };Ge(function (t) {
    t.setUTCSeconds(0, 0);
  }, function (t, n) {
    t.setTime(+t + n * tr);
  }, function (t, n) {
    return (n - t) / tr;
  }, function (t) {
    return t.getUTCMinutes();
  }), Ge(function (t) {
    t.setUTCMinutes(0, 0, 0);
  }, function (t, n) {
    t.setTime(+t + n * nr);
  }, function (t, n) {
    return (n - t) / nr;
  }, function (t) {
    return t.getUTCHours();
  });var cr = Ge(function (t) {
    t.setUTCHours(0, 0, 0, 0);
  }, function (t, n) {
    t.setUTCDate(t.getUTCDate() + n);
  }, function (t, n) {
    return (n - t) / 864e5;
  }, function (t) {
    return t.getUTCDate() - 1;
  });function fr(n) {
    return Ge(function (t) {
      t.setUTCDate(t.getUTCDate() - (t.getUTCDay() + 7 - n) % 7), t.setUTCHours(0, 0, 0, 0);
    }, function (t, n) {
      t.setUTCDate(t.getUTCDate() + 7 * n);
    }, function (t, n) {
      return (n - t) / 6048e5;
    });
  }var sr = fr(0),
      hr = fr(1),
      lr = (fr(2), fr(3), fr(4)),
      dr = (fr(5), fr(6), Ge(function (t) {
    t.setUTCDate(1), t.setUTCHours(0, 0, 0, 0);
  }, function (t, n) {
    t.setUTCMonth(t.getUTCMonth() + n);
  }, function (t, n) {
    return n.getUTCMonth() - t.getUTCMonth() + 12 * (n.getUTCFullYear() - t.getUTCFullYear());
  }, function (t) {
    return t.getUTCMonth();
  }), Ge(function (t) {
    t.setUTCMonth(0, 1), t.setUTCHours(0, 0, 0, 0);
  }, function (t, n) {
    t.setUTCFullYear(t.getUTCFullYear() + n);
  }, function (t, n) {
    return n.getUTCFullYear() - t.getUTCFullYear();
  }, function (t) {
    return t.getUTCFullYear();
  }));function pr(t) {
    if (0 <= t.y && t.y < 100) {
      var n = new Date(-1, t.m, t.d, t.H, t.M, t.S, t.L);return n.setFullYear(t.y), n;
    }return new Date(t.y, t.m, t.d, t.H, t.M, t.S, t.L);
  }function vr(t) {
    if (0 <= t.y && t.y < 100) {
      var n = new Date(Date.UTC(-1, t.m, t.d, t.H, t.M, t.S, t.L));return n.setUTCFullYear(t.y), n;
    }return new Date(Date.UTC(t.y, t.m, t.d, t.H, t.M, t.S, t.L));
  }function yr(t) {
    return { y: t, m: 0, d: 1, H: 0, M: 0, S: 0, L: 0 };
  }dr.every = function (e) {
    return isFinite(e = Math.floor(e)) && 0 < e ? Ge(function (t) {
      t.setUTCFullYear(Math.floor(t.getUTCFullYear() / e) * e), t.setUTCMonth(0, 1), t.setUTCHours(0, 0, 0, 0);
    }, function (t, n) {
      t.setUTCFullYear(t.getUTCFullYear() + n * e);
    }) : null;
  };var gr,
      br,
      mr,
      _r = { "-": "", _: " ", 0: "0" },
      wr = /^\s*\d+/,
      xr = /^%/,
      Mr = /[\\^$*+?|[\]().{}]/g;function Tr(t, n, e) {
    var r = t < 0 ? "-" : "",
        i = (r ? -t : t) + "",
        o = i.length;return r + (o < e ? new Array(e - o + 1).join(n) + i : i);
  }function Nr(t) {
    return t.replace(Mr, "\\$&");
  }function Cr(t) {
    return new RegExp("^(?:" + t.map(Nr).join("|") + ")", "i");
  }function kr(t) {
    for (var n = {}, e = -1, r = t.length; ++e < r;) {
      n[t[e].toLowerCase()] = e;
    }return n;
  }function Ar(t, n, e) {
    var r = wr.exec(n.slice(e, e + 1));return r ? (t.w = +r[0], e + r[0].length) : -1;
  }function Ur(t, n, e) {
    var r = wr.exec(n.slice(e, e + 1));return r ? (t.u = +r[0], e + r[0].length) : -1;
  }function Dr(t, n, e) {
    var r = wr.exec(n.slice(e, e + 2));return r ? (t.U = +r[0], e + r[0].length) : -1;
  }function Sr(t, n, e) {
    var r = wr.exec(n.slice(e, e + 2));return r ? (t.V = +r[0], e + r[0].length) : -1;
  }function zr(t, n, e) {
    var r = wr.exec(n.slice(e, e + 2));return r ? (t.W = +r[0], e + r[0].length) : -1;
  }function Er(t, n, e) {
    var r = wr.exec(n.slice(e, e + 4));return r ? (t.y = +r[0], e + r[0].length) : -1;
  }function Yr(t, n, e) {
    var r = wr.exec(n.slice(e, e + 2));return r ? (t.y = +r[0] + (68 < +r[0] ? 1900 : 2e3), e + r[0].length) : -1;
  }function Fr(t, n, e) {
    var r = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(n.slice(e, e + 6));return r ? (t.Z = r[1] ? 0 : -(r[2] + (r[3] || "00")), e + r[0].length) : -1;
  }function qr(t, n, e) {
    var r = wr.exec(n.slice(e, e + 2));return r ? (t.m = r[0] - 1, e + r[0].length) : -1;
  }function Hr(t, n, e) {
    var r = wr.exec(n.slice(e, e + 2));return r ? (t.d = +r[0], e + r[0].length) : -1;
  }function Pr(t, n, e) {
    var r = wr.exec(n.slice(e, e + 3));return r ? (t.m = 0, t.d = +r[0], e + r[0].length) : -1;
  }function Xr(t, n, e) {
    var r = wr.exec(n.slice(e, e + 2));return r ? (t.H = +r[0], e + r[0].length) : -1;
  }function Lr(t, n, e) {
    var r = wr.exec(n.slice(e, e + 2));return r ? (t.M = +r[0], e + r[0].length) : -1;
  }function jr(t, n, e) {
    var r = wr.exec(n.slice(e, e + 2));return r ? (t.S = +r[0], e + r[0].length) : -1;
  }function Or(t, n, e) {
    var r = wr.exec(n.slice(e, e + 3));return r ? (t.L = +r[0], e + r[0].length) : -1;
  }function Ir(t, n, e) {
    var r = wr.exec(n.slice(e, e + 6));return r ? (t.L = Math.floor(r[0] / 1e3), e + r[0].length) : -1;
  }function Vr(t, n, e) {
    var r = xr.exec(n.slice(e, e + 1));return r ? e + r[0].length : -1;
  }function Rr(t, n, e) {
    var r = wr.exec(n.slice(e));return r ? (t.Q = +r[0], e + r[0].length) : -1;
  }function Br(t, n, e) {
    var r = wr.exec(n.slice(e));return r ? (t.Q = 1e3 * +r[0], e + r[0].length) : -1;
  }function $r(t, n) {
    return Tr(t.getDate(), n, 2);
  }function Zr(t, n) {
    return Tr(t.getHours(), n, 2);
  }function Wr(t, n) {
    return Tr(t.getHours() % 12 || 12, n, 2);
  }function Qr(t, n) {
    return Tr(1 + er.count(ur(t), t), n, 3);
  }function Jr(t, n) {
    return Tr(t.getMilliseconds(), n, 3);
  }function Gr(t, n) {
    return Jr(t, n) + "000";
  }function Kr(t, n) {
    return Tr(t.getMonth() + 1, n, 2);
  }function ti(t, n) {
    return Tr(t.getMinutes(), n, 2);
  }function ni(t, n) {
    return Tr(t.getSeconds(), n, 2);
  }function ei(t) {
    var n = t.getDay();return 0 === n ? 7 : n;
  }function ri(t, n) {
    return Tr(ir.count(ur(t), t), n, 2);
  }function ii(t, n) {
    var e = t.getDay();return t = 4 <= e || 0 === e ? ar(t) : ar.ceil(t), Tr(ar.count(ur(t), t) + (4 === ur(t).getDay()), n, 2);
  }function oi(t) {
    return t.getDay();
  }function ai(t, n) {
    return Tr(or.count(ur(t), t), n, 2);
  }function ui(t, n) {
    return Tr(t.getFullYear() % 100, n, 2);
  }function ci(t, n) {
    return Tr(t.getFullYear() % 1e4, n, 4);
  }function fi(t) {
    var n = t.getTimezoneOffset();return (0 < n ? "-" : (n *= -1, "+")) + Tr(n / 60 | 0, "0", 2) + Tr(n % 60, "0", 2);
  }function si(t, n) {
    return Tr(t.getUTCDate(), n, 2);
  }function hi(t, n) {
    return Tr(t.getUTCHours(), n, 2);
  }function li(t, n) {
    return Tr(t.getUTCHours() % 12 || 12, n, 2);
  }function di(t, n) {
    return Tr(1 + cr.count(dr(t), t), n, 3);
  }function pi(t, n) {
    return Tr(t.getUTCMilliseconds(), n, 3);
  }function vi(t, n) {
    return pi(t, n) + "000";
  }function yi(t, n) {
    return Tr(t.getUTCMonth() + 1, n, 2);
  }function gi(t, n) {
    return Tr(t.getUTCMinutes(), n, 2);
  }function bi(t, n) {
    return Tr(t.getUTCSeconds(), n, 2);
  }function mi(t) {
    var n = t.getUTCDay();return 0 === n ? 7 : n;
  }function _i(t, n) {
    return Tr(sr.count(dr(t), t), n, 2);
  }function wi(t, n) {
    var e = t.getUTCDay();return t = 4 <= e || 0 === e ? lr(t) : lr.ceil(t), Tr(lr.count(dr(t), t) + (4 === dr(t).getUTCDay()), n, 2);
  }function xi(t) {
    return t.getUTCDay();
  }function Mi(t, n) {
    return Tr(hr.count(dr(t), t), n, 2);
  }function Ti(t, n) {
    return Tr(t.getUTCFullYear() % 100, n, 2);
  }function Ni(t, n) {
    return Tr(t.getUTCFullYear() % 1e4, n, 4);
  }function Ci() {
    return "+0000";
  }function ki() {
    return "%";
  }function Ai(t) {
    return +t;
  }function Ui(t) {
    return Math.floor(+t / 1e3);
  }gr = function (t) {
    var r = t.dateTime,
        i = t.date,
        o = t.time,
        n = t.periods,
        e = t.days,
        _a = t.shortDays,
        u = t.months,
        c = t.shortMonths,
        f = Cr(n),
        s = kr(n),
        h = Cr(e),
        l = kr(e),
        d = Cr(_a),
        p = kr(_a),
        v = Cr(u),
        y = kr(u),
        g = Cr(c),
        _b = kr(c),
        m = { a: function a(t) {
        return _a[t.getDay()];
      }, A: function A(t) {
        return e[t.getDay()];
      }, b: function b(t) {
        return c[t.getMonth()];
      }, B: function B(t) {
        return u[t.getMonth()];
      }, c: null, d: $r, e: $r, f: Gr, H: Zr, I: Wr, j: Qr, L: Jr, m: Kr, M: ti, p: function p(t) {
        return n[+(12 <= t.getHours())];
      }, Q: Ai, s: Ui, S: ni, u: ei, U: ri, V: ii, w: oi, W: ai, x: null, X: null, y: ui, Y: ci, Z: fi, "%": ki },
        _ = { a: function a(t) {
        return _a[t.getUTCDay()];
      }, A: function A(t) {
        return e[t.getUTCDay()];
      }, b: function b(t) {
        return c[t.getUTCMonth()];
      }, B: function B(t) {
        return u[t.getUTCMonth()];
      }, c: null, d: si, e: si, f: vi, H: hi, I: li, j: di, L: pi, m: yi, M: gi, p: function p(t) {
        return n[+(12 <= t.getUTCHours())];
      }, Q: Ai, s: Ui, S: bi, u: mi, U: _i, V: wi, w: xi, W: Mi, x: null, X: null, y: Ti, Y: Ni, Z: Ci, "%": ki },
        w = { a: function a(t, n, e) {
        var r = d.exec(n.slice(e));return r ? (t.w = p[r[0].toLowerCase()], e + r[0].length) : -1;
      }, A: function A(t, n, e) {
        var r = h.exec(n.slice(e));return r ? (t.w = l[r[0].toLowerCase()], e + r[0].length) : -1;
      }, b: function b(t, n, e) {
        var r = g.exec(n.slice(e));return r ? (t.m = _b[r[0].toLowerCase()], e + r[0].length) : -1;
      }, B: function B(t, n, e) {
        var r = v.exec(n.slice(e));return r ? (t.m = y[r[0].toLowerCase()], e + r[0].length) : -1;
      }, c: function c(t, n, e) {
        return T(t, r, n, e);
      }, d: Hr, e: Hr, f: Ir, H: Xr, I: Xr, j: Pr, L: Or, m: qr, M: Lr, p: function p(t, n, e) {
        var r = f.exec(n.slice(e));return r ? (t.p = s[r[0].toLowerCase()], e + r[0].length) : -1;
      }, Q: Rr, s: Br, S: jr, u: Ur, U: Dr, V: Sr, w: Ar, W: zr, x: function x(t, n, e) {
        return T(t, i, n, e);
      }, X: function X(t, n, e) {
        return T(t, o, n, e);
      }, y: Yr, Y: Er, Z: Fr, "%": Vr };function x(c, f) {
      return function (t) {
        var n,
            e,
            r,
            i = [],
            o = -1,
            a = 0,
            u = c.length;for (t instanceof Date || (t = new Date(+t)); ++o < u;) {
          37 === c.charCodeAt(o) && (i.push(c.slice(a, o)), null != (e = _r[n = c.charAt(++o)]) ? n = c.charAt(++o) : e = "e" === n ? " " : "0", (r = f[n]) && (n = r(t, e)), i.push(n), a = o + 1);
        }return i.push(c.slice(a, o)), i.join("");
      };
    }function M(i, o) {
      return function (t) {
        var n,
            e,
            r = yr(1900);if (T(r, i, t += "", 0) != t.length) return null;if ("Q" in r) return new Date(r.Q);if ("p" in r && (r.H = r.H % 12 + 12 * r.p), "V" in r) {
          if (r.V < 1 || 53 < r.V) return null;"w" in r || (r.w = 1), "Z" in r ? (n = vr(yr(r.y)), er = n.getUTCDay(), n = 4 < e || 0 === e ? hr.ceil(n) : hr(n), n = cr.offset(n, 7 * (r.V - 1)), r.y = n.getUTCFullYear(), r.m = n.getUTCMonth(), r.d = n.getUTCDate() + (r.w + 6) % 7) : (n = o(yr(r.y)), er = n.getDay(), n = 4 < e || 0 === e ? or.ceil(n) : or(n), n = er.offset(n, 7 * (r.V - 1)), r.y = n.getFullYear(), r.m = n.getMonth(), r.d = n.getDate() + (r.w + 6) % 7);
        } else ("W" in r || "U" in r) && ("w" in r || (r.w = "u" in r ? r.u % 7 : "W" in r ? 1 : 0), e = "Z" in r ? vr(yr(r.y)).getUTCDay() : o(yr(r.y)).getDay(), r.m = 0, r.d = "W" in r ? (r.w + 6) % 7 + 7 * r.W - (e + 5) % 7 : r.w + 7 * r.U - (e + 6) % 7);return "Z" in r ? (r.H += r.Z / 100 | 0, r.M += r.Z % 100, vr(r)) : o(r);
      };
    }function T(t, n, e, r) {
      for (var i, o, a = 0, u = n.length, c = e.length; a < u;) {
        if (c <= r) return -1;if (37 === (i = n.charCodeAt(a++))) {
          if (i = n.charAt(a++), !(o = w[i in _r ? n.charAt(a++) : i]) || (r = o(t, e, r)) < 0) return -1;
        } else if (i != e.charCodeAt(r++)) return -1;
      }return r;
    }return m.x = x(i, m), m.X = x(o, m), m.c = x(r, m), _.x = x(i, _), _.X = x(o, _), _.c = x(r, _), { format: function format(t) {
        var n = x(t += "", m);return n.toString = function () {
          return t;
        }, n;
      }, parse: function parse(t) {
        var n = M(t += "", pr);return n.toString = function () {
          return t;
        }, n;
      }, utcFormat: function utcFormat(t) {
        var n = x(t += "", _);return n.toString = function () {
          return t;
        }, n;
      }, utcParse: function utcParse(t) {
        var n = M(t, vr);return n.toString = function () {
          return t;
        }, n;
      } };
  }({ dateTime: "%x, %X", date: "%-m/%-d/%Y", time: "%-I:%M:%S %p", periods: ["AM", "PM"], days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"], shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"], months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"], shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"] }), br = gr.utcFormat, mr = gr.utcParse;var Di = "%Y-%m-%dT%H:%M:%S.%LZ";Date.prototype.toISOString || br(Di);+new Date("2000-01-01T00:00:00.000Z") || mr(Di);var Si = function Si(t) {
    return t.match(/.{6}/g).map(function (t) {
      return "#" + t;
    });
  };Si("1f77b4ff7f0e2ca02cd627289467bd8c564be377c27f7f7fbcbd2217becf"), Si("393b795254a36b6ecf9c9ede6379398ca252b5cf6bcedb9c8c6d31bd9e39e7ba52e7cb94843c39ad494ad6616be7969c7b4173a55194ce6dbdde9ed6"), Si("3182bd6baed69ecae1c6dbefe6550dfd8d3cfdae6bfdd0a231a35474c476a1d99bc7e9c0756bb19e9ac8bcbddcdadaeb636363969696bdbdbdd9d9d9"), Si("1f77b4aec7e8ff7f0effbb782ca02c98df8ad62728ff98969467bdc5b0d58c564bc49c94e377c2f7b6d27f7f7fc7c7c7bcbd22dbdb8d17becf9edae5"), Un(en(300, .5, 0), en(-240, .5, 1));Un(en(-100, .75, .35), en(80, 1.5, .8)), Un(en(260, .75, .35), en(80, 1.5, .8)), en();function zi(n) {
    var e = n.length;return function (t) {
      return n[Math.max(0, Math.min(e - 1, Math.floor(t * e)))];
    };
  }zi(Si(""));zi(Si("")), zi(Si("")), zi(Si(""));function Ei(t) {
    return t < 0 ? -1 : 1;
  }function Yi(t, n, e) {
    var r = t._x1 - t._x0,
        i = n - t._x1,
        o = (t._y1 - t._y0) / (r || i < 0 && -0),
        a = (e - t._y1) / (i || r < 0 && -0),
        u = (o * i + a * r) / (r + i);return (Ei(o) + Ei(a)) * Math.min(Math.abs(o), Math.abs(a), .5 * Math.abs(u)) || 0;
  }function Fi(t, n) {
    var e = t._x1 - t._x0;return e ? (3 * (t._y1 - t._y0) / e - n) / 2 : n;
  }function qi(t, n, e) {
    var r = t._x0,
        i = t._y0,
        o = t._x1,
        a = t._y1,
        u = (o - r) / 3;t._context.bezierCurveTo(r + u, i + u * n, o - u, a - u * e, o, a);
  }function Hi(t) {
    this._context = t;
  }function Pi(t) {
    this._context = t;
  }Hi.prototype = { areaStart: function areaStart() {
      this._line = 0;
    }, areaEnd: function areaEnd() {
      this._line = NaN;
    }, lineStart: function lineStart() {
      this._x0 = this._x1 = this._y0 = this._y1 = this._t0 = NaN, this._point = 0;
    }, lineEnd: function lineEnd() {
      switch (this._point) {case 2:
          this._context.lineTo(this._x1, this._y1);break;case 3:
          qi(this, this._t0, Fi(this, this._t0));}(this._line || 0 !== this._line && 1 === this._point) && this._context.closePath(), this._line = 1 - this._line;
    }, point: function point(t, n) {
      var e = NaN;if (n = +n, (t = +t) !== this._x1 || n !== this._y1) {
        switch (this._point) {case 0:
            this._point = 1, this._line ? this._context.lineTo(t, n) : this._context.moveTo(t, n);break;case 1:
            this._point = 2;break;case 2:
            this._point = 3, qi(this, Fi(this, e = Yi(this, t, n)), e);break;default:
            qi(this, this._t0, e = Yi(this, t, n));}this._x0 = this._x1, this._x1 = t, this._y0 = this._y1, this._y1 = n, this._t0 = e;
      }
    } }, (function (t) {
    this._context = new Pi(t);
  }.prototype = Object.create(Hi.prototype)).point = function (t, n) {
    Hi.prototype.point.call(this, n, t);
  }, Pi.prototype = { moveTo: function moveTo(t, n) {
      this._context.moveTo(n, t);
    }, closePath: function closePath() {
      this._context.closePath();
    }, lineTo: function lineTo(t, n) {
      this._context.lineTo(n, t);
    }, bezierCurveTo: function bezierCurveTo(t, n, e, r, i, o) {
      this._context.bezierCurveTo(n, t, r, e, o, i);
    } };function Xi(t, n, e) {
    this.target = t, this.type = n, this.transform = e;
  }function Li(t, n, e) {
    this.k = t, this.x = n, this.y = e;
  }Li.prototype = { constructor: Li, scale: function scale(t) {
      return 1 === t ? this : new Li(this.k * t, this.x, this.y);
    }, translate: function translate(t, n) {
      return 0 === t & 0 === n ? this : new Li(this.k, this.x + this.k * t, this.y + this.k * n);
    }, apply: function apply(t) {
      return [t[0] * this.k + this.x, t[1] * this.k + this.y];
    }, applyX: function applyX(t) {
      return t * this.k + this.x;
    }, applyY: function applyY(t) {
      return t * this.k + this.y;
    }, invert: function invert(t) {
      return [(t[0] - this.x) / this.k, (t[1] - this.y) / this.k];
    }, invertX: function invertX(t) {
      return (t - this.x) / this.k;
    }, invertY: function invertY(t) {
      return (t - this.y) / this.k;
    }, rescaleX: function rescaleX(t) {
      return t.copy().domain(t.range().map(this.invertX, this).map(t.invert, t));
    }, rescaleY: function rescaleY(t) {
      return t.copy().domain(t.range().map(this.invertY, this).map(t.invert, t));
    }, toString: function toString() {
      return "translate(" + this.x + "," + this.y + ") scale(" + this.k + ")";
    } };var ji = new Li(1, 0, 0);function Oi() {
    D.event.stopImmediatePropagation();
  }var Ii = function Ii() {
    D.event.preventDefault(), D.event.stopImmediatePropagation();
  };function Vi() {
    return !D.event.button;
  }function Ri() {
    var t,
        n,
        e = this;return e instanceof SVGElement ? (t = (e = e.ownerSVGElement || e).width.baseVal.value, n = e.height.baseVal.value) : (t = e.clientWidth, n = e.clientHeight), [[0, 0], [t, n]];
  }function Bi() {
    return this.__zoom || ji;
  }function $i() {
    return -D.event.deltaY * (D.event.deltaMode ? 120 : 1) / 500;
  }function Zi() {
    return "ontouchstart" in this;
  }function Wi(t, n, e) {
    var r = t.invertX(n[0][0]) - e[0][0],
        i = t.invertX(n[1][0]) - e[1][0],
        o = t.invertY(n[0][1]) - e[0][1],
        a = t.invertY(n[1][1]) - e[1][1];return t.translate(r < i ? (r + i) / 2 : Math.min(0, r) || Math.max(0, i), o < a ? (o + a) / 2 : Math.min(0, o) || Math.max(0, a));
  }D.forceCollide = function (r) {
    var o,
        a,
        y = 1,
        u = 1;function n() {
      for (var t, n, h, l, d, p, v, e = o.length, r = 0; r < u; ++r) {
        for (n = Ne(o, Ue, De).visitAfter(c), t = 0; t < e; ++t) {
          h = o[t], p = a[h.index], v = p * p, l = h.x + h.vx, d = h.y + h.vy, n.visit(i);
        }
      }function i(t, n, e, r, i) {
        var o = t.data,
            a = t.r,
            u = p + a;if (!o) return l + u < n || r < l - u || d + u < e || i < d - u;if (o.index > h.index) {
          var c = l - o.x - o.vx,
              f = d - o.y - o.vy,
              s = c * c + f * f;s < u * u && (0 === c && (s += (c = _e()) * c), 0 === f && (s += (f = _e()) * f), s = (u - (s = Math.sqrt(s))) / s * y, h.vx += (c *= s) * (u = (a *= a) / (v + a)), h.vy += (f *= s) * u, o.vx -= c * (u = 1 - u), o.vy -= f * u);
        }
      }
    }function c(t) {
      if (t.data) return t.r = a[t.data.index];for (var n = t.r = 0; n < 4; ++n) {
        t[n] && t[n].r > t.r && (t.r = t[n].r);
      }
    }function e() {
      if (o) {
        var t,
            n,
            e = o.length;for (a = new Array(e), t = 0; t < e; ++t) {
          n = o[t], a[n.index] = +r(n, t, o);
        }
      }
    }return "function" != typeof r && (r = me(null == r ? 1 : +r)), n.initialize = function (t) {
      o = t, e();
    }, n.iterations = function (t) {
      return arguments.length ? (u = +t, n) : u;
    }, n.strength = function (t) {
      return arguments.length ? (y = +t, n) : y;
    }, n.radius = function (t) {
      return arguments.length ? (r = "function" == typeof t ? t : constant(+t), e(), n) : r;
    }, n;
  }, D.forceLink = function (h) {
    var l,
        d,
        o,
        a,
        p,
        u = Se,
        e = function e(t) {
      return 1 / Math.min(a[t.source.index], a[t.target.index]);
    },
        r = me(30),
        v = 1;function n(t) {
      for (var n = 0, e = h.length; n < v; ++n) {
        for (var r, i, o, a, u, c, f, s = 0; s < e; ++s) {
          i = (r = h[s]).source, a = (o = r.target).x + o.vx - i.x - i.vx || _e(), u = o.y + o.vy - i.y - i.vy || _e(), a *= c = ((c = Math.sqrt(a * a + u * u)) - d[s]) / c * t * l[s], u *= c, o.vx -= a * (f = p[s]), o.vy -= u * f, i.vx += a * (f = 1 - f), i.vy += u * f;
        }
      }
    }function i() {
      if (o) {
        var t,
            n,
            e = o.length,
            r = h.length,
            i = pe(o, u);for (t = 0, a = new Array(e); t < r; ++t) {
          (n = h[t]).index = t, "object" != _typeof(n.source) && (n.source = ze(i, n.source)), "object" != _typeof(n.target) && (n.target = ze(i, n.target)), a[n.source.index] = (a[n.source.index] || 0) + 1, a[n.target.index] = (a[n.target.index] || 0) + 1;
        }for (t = 0, p = new Array(r); t < r; ++t) {
          n = h[t], p[t] = a[n.source.index] / (a[n.source.index] + a[n.target.index]);
        }l = new Array(r), c(), d = new Array(r), f();
      }
    }function c() {
      if (o) for (var t = 0, n = h.length; t < n; ++t) {
        l[t] = +e(h[t], t, h);
      }
    }function f() {
      if (o) for (var t = 0, n = h.length; t < n; ++t) {
        d[t] = +r(h[t], t, h);
      }
    }return null == h && (h = []), n.initialize = function (t) {
      o = t, i();
    }, n.links = function (t) {
      return arguments.length ? (h = t, i(), n) : h;
    }, n.id = function (t) {
      return arguments.length ? (u = t, n) : u;
    }, n.iterations = function (t) {
      return arguments.length ? (v = +t, n) : v;
    }, n.strength = function (t) {
      return arguments.length ? (e = "function" == typeof t ? t : constant(+t), c(), n) : e;
    }, n.distance = function (t) {
      return arguments.length ? (r = "function" == typeof t ? t : constant(+t), f(), n) : r;
    }, n;
  }, D.forceManyBody = function () {
    var i,
        c,
        f,
        s,
        r = me(-30),
        h = 1,
        l = 1 / 0,
        d = .81;function n(t) {
      var n,
          e = i.length,
          r = Ne(i, Ee, Ye).visitAfter(o);for (f = t, n = 0; n < e; ++n) {
        c = i[n], r.visit(a);
      }
    }function e() {
      if (i) {
        var t,
            n,
            e = i.length;for (s = new Array(e), t = 0; t < e; ++t) {
          n = i[t], s[n.index] = +r(n, t, i);
        }
      }
    }function o(t) {
      var n,
          e,
          r,
          i,
          o,
          a = 0,
          u = 0;if (t.length) {
        for (r = i = o = 0; o < 4; ++o) {
          (n = t[o]) && (e = Math.abs(n.value)) && (a += n.value, u += e, r += e * n.x, i += e * n.y);
        }t.x = r / u, t.y = i / u;
      } else for ((n = t).x = n.data.x, n.y = n.data.y; a += s[n.data.index], n = n.next;) {}t.value = a;
    }function a(t, n, e, r) {
      if (!t.value) return !0;var i = t.x - c.x,
          o = t.y - c.y,
          a = r - n,
          u = i * i + o * o;if (a * a / d < u) return u < l && (0 === i && (u += (i = _e()) * i), 0 === o && (u += (o = _e()) * o), u < h && (u = Math.sqrt(h * u)), c.vx += i * t.value * f / u, c.vy += o * t.value * f / u), !0;if (!(t.length || l <= u)) for ((t.data !== c || t.next) && (0 === i && (u += (i = _e()) * i), 0 === o && (u += (o = _e()) * o), u < h && (u = Math.sqrt(h * u))); t.data !== c && (a = s[t.data.index] * f / u, c.vx += i * a, c.vy += o * a), t = t.next;) {}
    }return n.initialize = function (t) {
      i = t, e();
    }, n.strength = function (t) {
      return arguments.length ? (r = "function" == typeof t ? t : constant(+t), e(), n) : r;
    }, n.distanceMin = function (t) {
      return arguments.length ? (h = t * t, n) : Math.sqrt(h);
    }, n.distanceMax = function (t) {
      return arguments.length ? (l = t * t, n) : Math.sqrt(l);
    }, n.theta = function (t) {
      return arguments.length ? (d = t * t, n) : Math.sqrt(d);
    }, n;
  }, D.forceSimulation = function (s) {
    var e,
        r = 1,
        n = .001,
        i = 1 - Math.pow(n, 1 / 300),
        o = 0,
        a = .6,
        u = pe(),
        t = On(f),
        c = S("tick", "end");function f() {
      h(), c.call("tick", e), r < n && (t.stop(), c.call("end", e));
    }function h() {
      var t,
          n,
          e = s.length;for (r += (o - r) * i, u.each(function (t) {
        t(r);
      }), t = 0; t < e; ++t) {
        null == (n = s[t]).fx ? n.x += n.vx *= a : (n.x = n.fx, n.vx = 0), null == n.fy ? n.y += n.vy *= a : (n.y = n.fy, n.vy = 0);
      }
    }function l() {
      for (var t, n = 0, e = s.length; n < e; ++n) {
        if ((t = s[n]).index = n, isNaN(t.x) || isNaN(t.y)) {
          var r = 10 * Math.sqrt(n),
              i = n * Fe;t.x = r * Math.cos(i), t.y = r * Math.sin(i);
        }(isNaN(t.vx) || isNaN(t.vy)) && (t.vx = t.vy = 0);
      }
    }function d(t) {
      return t.initialize && t.initialize(s), t;
    }return null == s && (s = []), l(), e = { tick: h, restart: function restart() {
        return t.restart(f), e;
      }, stop: function stop() {
        return t.stop(), e;
      }, nodes: function nodes(t) {
        return arguments.length ? (s = t, l(), u.each(d), e) : s;
      }, alpha: function alpha(t) {
        return arguments.length ? (r = +t, e) : r;
      }, alphaMin: function alphaMin(t) {
        return arguments.length ? (n = +t, e) : n;
      }, alphaDecay: function alphaDecay(t) {
        return arguments.length ? (i = +t, e) : +i;
      }, alphaTarget: function alphaTarget(t) {
        return arguments.length ? (o = +t, e) : o;
      }, velocityDecay: function velocityDecay(t) {
        return arguments.length ? (a = 1 - t, e) : 1 - a;
      }, force: function force(t, n) {
        return 1 < arguments.length ? (null == n ? u.remove(t) : u.set(t, d(n)), e) : u.get(t);
      }, find: function find(t, n, e) {
        var r,
            i,
            o,
            a,
            u,
            c = 0,
            f = s.length;for (null == e ? e = 1 / 0 : e *= e, c = 0; c < f; ++c) {
          (o = (r = t - (a = s[c]).x) * r + (i = n - a.y) * i) < e && (u = a, e = o);
        }return u;
      }, on: function on(t, n) {
        return 1 < arguments.length ? (c.on(t, n), e) : c.on(t);
      } };
  }, D.select = Z, D.drag = function () {
    var e,
        r,
        i,
        o,
        a = it,
        u = ot,
        n = at,
        c = ut,
        p = {},
        v = S("start", "drag", "end"),
        y = 0,
        f = 0;function g(t) {
      t.on("mousedown.drag", s).filter(c).on("touchstart.drag", d).on("touchmove.drag", b).on("touchend.drag touchcancel.drag", m).style("touch-action", "none").style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
    }function s() {
      if (!o && a.apply(this, arguments)) {
        var t = _("mouse", u.apply(this, arguments), J, this, arguments);t && (Z(D.event.view).on("mousemove.drag", h, !0).on("mouseup.drag", l, !0), nt(D.event.view), K(), i = !1, e = D.event.clientX, r = D.event.clientY, t("start"));
      }
    }function h() {
      if (tt(), !i) {
        var t = D.event.clientX - e,
            n = D.event.clientY - r;i = f < t * t + n * n;
      }p.mouse("drag");
    }function l() {
      Z(D.event.view).on("mousemove.drag mouseup.drag", null), et(D.event.view, i), tt(), p.mouse("end");
    }function d() {
      if (a.apply(this, arguments)) {
        var t,
            n,
            e = D.event.changedTouches,
            r = u.apply(this, arguments),
            i = e.length;for (t = 0; t < i; ++t) {
          (n = _(e[t].identifier, r, G, this, arguments)) && (K(), n("start"));
        }
      }
    }function b() {
      var t,
          n,
          e = D.event.changedTouches,
          r = e.length;for (t = 0; t < r; ++t) {
        (n = p[e[t].identifier]) && (tt(), n("drag"));
      }
    }function m() {
      var t,
          n,
          e = D.event.changedTouches,
          r = e.length;for (o && clearTimeout(o), o = setTimeout(function () {
        o = null;
      }, 500), t = 0; t < r; ++t) {
        (n = p[e[t].identifier]) && (K(), n("end"));
      }
    }function _(i, o, a, u, c) {
      var f,
          s,
          h,
          l = a(o, i),
          d = v.copy();if (I(new rt(g, "beforestart", f, i, y, l[0], l[1], 0, 0, d), function () {
        return null != (D.event.subject = f = n.apply(u, c)) && (s = f.x - l[0] || 0, h = f.y - l[1] || 0, !0);
      })) return function t(n) {
        var e,
            r = l;switch (n) {case "start":
            p[i] = t, e = y++;break;case "end":
            delete p[i], --y;case "drag":
            l = a(o, i), e = y;}I(new rt(g, n, f, i, e, l[0] + s, l[1] + h, l[0] - r[0], l[1] - r[1], d), d.apply, d, [n, u, c]);
      };
    }return g.filter = function (t) {
      return arguments.length ? (a = "function" == typeof t ? t : constant(!!t), g) : a;
    }, g.container = function (t) {
      return arguments.length ? (u = "function" == typeof t ? t : constant(t), g) : u;
    }, g.subject = function (t) {
      return arguments.length ? (n = "function" == typeof t ? t : constant(t), g) : n;
    }, g.touchable = function (t) {
      return arguments.length ? (c = "function" == typeof t ? t : constant(!!t), g) : c;
    }, g.on = function () {
      var t = v.on.apply(v, arguments);return t === v ? g : t;
    }, g.clickDistance = function (t) {
      return arguments.length ? (f = (t = +t) * t, g) : Math.sqrt(f);
    }, g;
  }, D.zoom = function () {
    var d,
        o,
        u = Vi,
        s = Ri,
        p = Wi,
        i = $i,
        n = Zi,
        a = [0, 1 / 0],
        v = [[-1 / 0, -1 / 0], [1 / 0, 1 / 0]],
        c = 250,
        h = Nn,
        f = [],
        e = S("start", "zoom", "end"),
        l = 500,
        y = 150,
        g = 0;function b(t) {
      t.property("__zoom", Bi).on("wheel.zoom", r).on("mousedown.zoom", N).on("dblclick.zoom", C).filter(n).on("touchstart.zoom", k).on("touchmove.zoom", A).on("touchend.zoom touchcancel.zoom", U).style("touch-action", "none").style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
    }function m(t, n) {
      return (n = Math.max(a[0], Math.min(a[1], n))) === t.k ? t : new Li(n, t.x, t.y);
    }function _(t, n, e) {
      var r = n[0] - e[0] * t.k,
          i = n[1] - e[1] * t.k;return r === t.x && i === t.y ? t : new Li(t.k, r, i);
    }function w(t) {
      return [(+t[0][0] + +t[1][0]) / 2, (+t[0][1] + +t[1][1]) / 2];
    }function x(t, c, f) {
      t.on("start.zoom", function () {
        M(this, arguments).start();
      }).on("interrupt.zoom end.zoom", function () {
        M(this, arguments).end();
      }).tween("zoom", function () {
        var t = arguments,
            r = M(this, t),
            n = s.apply(this, t),
            i = f || w(n),
            o = Math.max(n[1][0] - n[0][0], n[1][1] - n[0][1]),
            e = this.__zoom,
            a = "function" == typeof c ? c.apply(this, t) : c,
            u = h(e.invert(i).concat(o / e.k), a.invert(i).concat(o / a.k));return function (t) {
          if (1 === t) t = a;else {
            var n = u(t),
                e = o / n[2];t = new Li(e, i[0] - n[0] * e, i[1] - n[1] * e);
          }r.zoom(null, t);
        };
      });
    }function M(t, n) {
      for (var e, r = 0, i = f.length; r < i; ++r) {
        if ((e = f[r]).that === t) return e;
      }return new T(t, n);
    }function T(t, n) {
      this.that = t, this.args = n, this.index = -1, this.active = 0, this.extent = s.apply(t, n);
    }function r() {
      if (u.apply(this, arguments)) {
        var t = M(this, arguments),
            n = this.__zoom,
            e = Math.max(a[0], Math.min(a[1], n.k * Math.pow(2, i.apply(this, arguments)))),
            r = J(this);if (t.wheel) t.mouse[0][0] === r[0] && t.mouse[0][1] === r[1] || (t.mouse[1] = n.invert(t.mouse[0] = r)), clearTimeout(t.wheel);else {
          if (n.k === e) return;t.mouse = [r, n.invert(r)], ne(this), t.start();
        }Ii(), t.wheel = setTimeout(function () {
          t.wheel = null, t.end();
        }, y), t.zoom("mouse", p(_(m(n, e), t.mouse[0], t.mouse[1]), t.extent, v));
      }
    }function N() {
      if (!o && u.apply(this, arguments)) {
        var e = M(this, arguments),
            t = Z(D.event.view).on("mousemove.zoom", function () {
          if (Ii(), !e.moved) {
            var t = D.event.clientX - r,
                n = D.event.clientY - i;e.moved = g < t * t + n * n;
          }e.zoom("mouse", p(_(e.that.__zoom, e.mouse[0] = J(e.that), e.mouse[1]), e.extent, v));
        }, !0).on("mouseup.zoom", function () {
          t.on("mousemove.zoom mouseup.zoom", null), et(D.event.view, e.moved), Ii(), e.end();
        }, !0),
            n = J(this),
            r = D.event.clientX,
            i = D.event.clientY;nt(D.event.view), Oi(), e.mouse = [n, this.__zoom.invert(n)], ne(this), e.start();
      }
    }function C() {
      if (u.apply(this, arguments)) {
        var t = this.__zoom,
            n = J(this),
            e = t.invert(n),
            r = t.k * (D.event.shiftKey ? .5 : 2),
            i = p(_(m(t, r), n, e), s.apply(this, arguments), v);Ii(), 0 < c ? Z(this).transition().duration(c).call(x, i, n) : Z(this).call(b.transform, i);
      }
    }function k() {
      if (u.apply(this, arguments)) {
        var t,
            n,
            e,
            r,
            i = M(this, arguments),
            o = D.event.changedTouches,
            a = o.length;for (Oi(), n = 0; n < a; ++n) {
          e = o[n], r = [r = G(this, o, e.identifier), this.__zoom.invert(r), e.identifier], i.touch0 ? i.touch1 || (i.touch1 = r) : (i.touch0 = r, t = !0);
        }if (d && (d = clearTimeout(d), !i.touch1)) return i.end(), void ((r = Z(this).on("dblclick.zoom")) && r.apply(this, arguments));t && (d = setTimeout(function () {
          d = null;
        }, l), ne(this), i.start());
      }
    }function A() {
      var t,
          n,
          e,
          r,
          i = M(this, arguments),
          o = D.event.changedTouches,
          a = o.length;for (Ii(), d && (d = clearTimeout(d)), t = 0; t < a; ++t) {
        n = o[t], e = G(this, o, n.identifier), i.touch0 && i.touch0[2] === n.identifier ? i.touch0[0] = e : i.touch1 && i.touch1[2] === n.identifier && (i.touch1[0] = e);
      }if (n = i.that.__zoom, i.touch1) {
        var u = i.touch0[0],
            c = i.touch0[1],
            f = i.touch1[0],
            s = i.touch1[1],
            h = (h = f[0] - u[0]) * h + (h = f[1] - u[1]) * h,
            l = (l = s[0] - c[0]) * l + (l = s[1] - c[1]) * l;n = m(n, Math.sqrt(h / l)), e = [(u[0] + f[0]) / 2, (u[1] + f[1]) / 2], r = [(c[0] + s[0]) / 2, (c[1] + s[1]) / 2];
      } else {
        if (!i.touch0) return;e = i.touch0[0], r = i.touch0[1];
      }i.zoom("touch", p(_(n, e, r), i.extent, v));
    }function U() {
      var t,
          n,
          e = M(this, arguments),
          r = D.event.changedTouches,
          i = r.length;for (Oi(), o && clearTimeout(o), o = setTimeout(function () {
        o = null;
      }, l), t = 0; t < i; ++t) {
        n = r[t], e.touch0 && e.touch0[2] === n.identifier ? delete e.touch0 : e.touch1 && e.touch1[2] === n.identifier && delete e.touch1;
      }e.touch1 && !e.touch0 && (e.touch0 = e.touch1, delete e.touch1), e.touch0 ? e.touch0[1] = this.__zoom.invert(e.touch0[0]) : e.end();
    }return b.transform = function (t, n) {
      var e = t.selection ? t.selection() : t;e.property("__zoom", Bi), t !== e ? x(t, n) : e.interrupt().each(function () {
        M(this, arguments).start().zoom(null, "function" == typeof n ? n.apply(this, arguments) : n).end();
      });
    }, b.scaleBy = function (t, n) {
      b.scaleTo(t, function () {
        return this.__zoom.k * ("function" == typeof n ? n.apply(this, arguments) : n);
      });
    }, b.scaleTo = function (t, o) {
      b.transform(t, function () {
        var t = s.apply(this, arguments),
            n = this.__zoom,
            e = w(t),
            r = n.invert(e),
            i = "function" == typeof o ? o.apply(this, arguments) : o;return p(_(m(n, i), e, r), t, v);
      });
    }, b.translateBy = function (t, n, e) {
      b.transform(t, function () {
        return p(this.__zoom.translate("function" == typeof n ? n.apply(this, arguments) : n, "function" == typeof e ? e.apply(this, arguments) : e), s.apply(this, arguments), v);
      });
    }, b.translateTo = function (t, r, i) {
      b.transform(t, function () {
        var t = s.apply(this, arguments),
            n = this.__zoom,
            e = w(t);return p(ji.translate(e[0], e[1]).scale(n.k).translate("function" == typeof r ? -r.apply(this, arguments) : -r, "function" == typeof i ? -i.apply(this, arguments) : -i), t, v);
      });
    }, T.prototype = { start: function start() {
        return 1 == ++this.active && (this.index = f.push(this) - 1, this.emit("start")), this;
      }, zoom: function zoom(t, n) {
        return this.mouse && "mouse" !== t && (this.mouse[1] = n.invert(this.mouse[0])), this.touch0 && "touch" !== t && (this.touch0[1] = n.invert(this.touch0[0])), this.touch1 && "touch" !== t && (this.touch1[1] = n.invert(this.touch1[0])), this.that.__zoom = n, this.emit("zoom"), this;
      }, end: function end() {
        return 0 == --this.active && (f.splice(this.index, 1), this.index = -1, this.emit("end")), this;
      }, emit: function emit(t) {
        I(new Xi(b, t, this.that.__zoom), e.apply, e, [t, this.that, this.args]);
      } }, b.wheelDelta = function (t) {
      return arguments.length ? (i = "function" == typeof t ? t : constant(+t), b) : i;
    }, b.filter = function (t) {
      return arguments.length ? (u = "function" == typeof t ? t : constant(!!t), b) : u;
    }, b.touchable = function (t) {
      return arguments.length ? (n = "function" == typeof t ? t : constant(!!t), b) : n;
    }, b.extent = function (t) {
      return arguments.length ? (s = "function" == typeof t ? t : constant([[+t[0][0], +t[0][1]], [+t[1][0], +t[1][1]]]), b) : s;
    }, b.scaleExtent = function (t) {
      return arguments.length ? (a[0] = +t[0], a[1] = +t[1], b) : [a[0], a[1]];
    }, b.translateExtent = function (t) {
      return arguments.length ? (v[0][0] = +t[0][0], v[1][0] = +t[1][0], v[0][1] = +t[0][1], v[1][1] = +t[1][1], b) : [[v[0][0], v[0][1]], [v[1][0], v[1][1]]];
    }, b.constrain = function (t) {
      return arguments.length ? (p = t, b) : p;
    }, b.duration = function (t) {
      return arguments.length ? (c = +t, b) : c;
    }, b.interpolate = function (t) {
      return arguments.length ? (h = t, b) : h;
    }, b.on = function () {
      var t = e.on.apply(e, arguments);return t === e ? b : t;
    }, b.clickDistance = function (t) {
      return arguments.length ? (g = (t = +t) * t, b) : Math.sqrt(g);
    }, b;
  }, D.zoomIdentity = ji, Object.defineProperty(D, "__esModule", { value: !0 });
});

/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
/*
 * Return the dimensions (width & height) that some HTML
 * with a given style would take in the page.
 */
var getDimensions = exports.getDimensions = function getDimensions(html, style, classname) {
  var el = document.createElement('span');
  var dimensions = {};

  // Set display: inline-block so that the size of el
  // will depend on the size of its children.
  el.style.display = 'inline-block';

  // Hide the element (it will be added to the page for a short time).
  el.style.visibility = 'hidden';

  el.className = classname;
  el.innerHTML = html;

  // Apply CSS rules.
  Object.keys(style).forEach(function (rule) {
    el.style[rule] = style[rule];
  });
  document.body.append(el);

  dimensions.width = el.offsetWidth;
  dimensions.height = el.offsetHeight;

  el.remove();
  return dimensions;
};

/*
 * Return the dimensions of an SVG viewport calculated from
 * some given nodes.
 */
var getViewBox = exports.getViewBox = function getViewBox(nodes) {
  var Xs = [];
  var Ys = [];

  nodes.forEach(function (node) {
    var x = node.x || node.fx;
    var y = node.y || node.fy;

    if (x) {
      Xs.push(x);
    }

    if (y) {
      Ys.push(y);
    }
  });

  if (Xs.length === 0 || Ys.length === 0) {
    return '0 0 0 0';
  }

  // Find the smallest coordinates...
  var min = [Math.min.apply(Math, Xs) - 150, Math.min.apply(Math, Ys) - 150];

  // ...and the biggest ones.
  var max = [Math.max.apply(Math, Xs) - min[0] + 150, Math.max.apply(Math, Ys) - min[1] + 150];

  return min.join(' ') + ' ' + max.join(' ');
};

/***/ }),
/* 4 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


// Regex that matches all emojis in a string.
var matchEmojis = /([\uD800-\uDBFF][\uDC00-\uDFFF])/g;

// Emoji to category table.
var conversionTable = {
  '🗺': 'mindmap',
  '🌐': 'wiki',
  '🗂': 'stack exchange',
  '📝': 'course',
  '📖': 'free book',
  '📕': 'non-free book',
  '📄': 'paper',
  '👀': 'video',
  '🖋': 'article',
  '🗃': 'blog',
  '🐙': 'github',
  '👾': 'interactive',
  '🖌': 'image',
  '🎙': 'podcast',
  '📮': 'newsletter',
  '💬': 'chat',
  '🎥': 'youtube',
  '🤖': 'reddit',
  '🔎': 'quora',
  '🔗': undefined
};

// Category to emoji table, based on the table above.
var revConversionTable = {};

Object.keys(conversionTable).forEach(function (key) {
  revConversionTable[conversionTable[key]] = key;
});

/*
 * Return an emoji as a GitHub image.
 */
var emojiTemplate = function emojiTemplate(unicode, category) {
  return '<img class="mindmap-emoji" title="' + category + '" src="https://assets-cdn.github.com/images/icons/emoji/unicode/' + unicode + '.png">';
};

var customEmojiTemplate = function customEmojiTemplate(emoji, category) {
  return '<img class="mindmap-emoji" title="' + category + '" src="https://assets-cdn.github.com/images/icons/emoji/' + emoji + '.png">';
};

/*
 * Return the category represented by the given emoji.
 */
var emojiToCategory = function emojiToCategory(emoji) {
  return conversionTable[emoji] || '';
};

/*
 * Convert all emojis to an IMG tag.
 * The bitwise magic is explained at http://crocodillon.com/blog/parsing-emoji-unicode-in-javascript
 */
var emojiToIMG = function emojiToIMG(html) {
  return (
    /* eslint-disable no-bitwise */
    html.replace(matchEmojis, function (match) {
      switch (match) {
        case '🤖':
          return '<img class="mindmap-emoji reddit-emoji" title="reddit" src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTNpOQVZdTCyVamjJPl92KjaDHigNWVM8mOLHPRU4DHoVNJWxCg">';

        case '🗂':
          return '<img class="mindmap-emoji" title="stackexchange" src="https://cdn.sstatic.net/Sites/stackoverflow/company/img/logos/se/se-icon.png?v=93426798a1d4">';

        case '🐙':
          return customEmojiTemplate('octocat', 'github');

        case '🔎':
          return '<img class="mindmap-emoji" title="quora" src="https://www.shareicon.net/data/2016/11/03/849470_social_512x512.png">';

        // Regular unicode Emojis.
        default:
          {
            // Keep the first 10 bits.
            var lead = match.charCodeAt(0) & 0x3FF;
            var trail = match.charCodeAt(1) & 0x3FF;

            // 0x[lead][trail]
            var unicode = ((lead << 10) + trail).toString(16);

            return emojiTemplate('1' + unicode, emojiToCategory(match));
          }
      }
    })
    /* eslint-enable no-bitwise */

  );
};

/*
 * Inverse of emojiToCategory, but instead of returning an emoji
 * returns an IMG tag corresponding to that emoji.
 */
var categoryToIMG = function categoryToIMG(category) {
  return emojiToIMG(revConversionTable[category] || '');
};

module.exports = {
  matchEmojis: matchEmojis,
  emojiToIMG: emojiToIMG,
  emojiTemplate: emojiTemplate,
  emojiToCategory: emojiToCategory,
  categoryToIMG: categoryToIMG
};

/***/ }),
/* 5 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _MindMap = __webpack_require__(6);

var _MindMap2 = _interopRequireDefault(_MindMap);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = _MindMap2.default;

/***/ }),
/* 6 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(7);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(8);

var _propTypes2 = _interopRequireDefault(_propTypes);

var _d = __webpack_require__(2);

var _d2 = __webpack_require__(13);

var _dimensions = __webpack_require__(3);

var _subnodesToHTML = __webpack_require__(14);

var _subnodesToHTML2 = _interopRequireDefault(_subnodesToHTML);

var _nodeToHTML = __webpack_require__(15);

var _nodeToHTML2 = _interopRequireDefault(_nodeToHTML);

__webpack_require__(16);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var MindMap = function (_Component) {
  _inherits(MindMap, _Component);

  function MindMap(props) {
    _classCallCheck(this, MindMap);

    // Create force simulation to position nodes that have no coordinates,
    // and add it to the state.
    var _this = _possibleConstructorReturn(this, (MindMap.__proto__ || Object.getPrototypeOf(MindMap)).call(this, props));

    var simulation = (0, _d.forceSimulation)().force('link', (0, _d.forceLink)().id(function (node) {
      return node.text;
    })).force('charge', (0, _d.forceManyBody)()).force('collide', (0, _d.forceCollide)().radius(100));

    _this.state = { simulation: simulation };
    return _this;
  }

  /* eslint-disable no-param-reassign */
  /*
   * Generates HTML and dimensions for nodes and subnodes.
   */


  _createClass(MindMap, [{
    key: 'prepareNodes',
    value: function prepareNodes() {
      var render = function render(node) {
        node.html = (0, _nodeToHTML2.default)(node);
        node.nodesHTML = (0, _subnodesToHTML2.default)(node.nodes);

        var dimensions = (0, _dimensions.getDimensions)(node.html, {}, 'mindmap-node');
        node.width = dimensions.width;
        node.height = dimensions.height;

        var nodesDimensions = (0, _dimensions.getDimensions)(node.nodesHTML, {}, 'mindmap-subnode-text');
        node.nodesWidth = nodesDimensions.width;
        node.nodesHeight = nodesDimensions.height;
      };

      this.props.nodes.forEach(function (node) {
        return render(node);
      });
    }

    /*
     * Add new class to nodes, attach drag behavior, and start simulation.
     */

  }, {
    key: 'prepareEditor',
    value: function prepareEditor(svg, conns, nodes, subnodes) {
      var _this2 = this;

      nodes.attr('class', 'mindmap-node mindmap-node--editable').on('dblclick', function (node) {
        node.fx = null;
        node.fy = null;
      });

      nodes.call((0, _d2.d3Drag)(this.state.simulation, svg, nodes));

      // Tick the simulation 100 times.
      for (var i = 0; i < 100; i += 1) {
        this.state.simulation.tick();
      }
      (0, _d2.onTick)(conns, nodes, subnodes);

      setTimeout(function () {
        _this2.state.simulation.alphaTarget(0.5).on('tick', function () {
          return (0, _d2.onTick)(conns, nodes, subnodes);
        });
      }, 200);
    }
    /* eslint-enable no-param-reassign */

    /*
     * Render mind map using D3.
     */

  }, {
    key: 'renderMap',
    value: function renderMap() {
      var svg = (0, _d.select)(this.mountPoint);

      // Clear the SVG in case there's stuff already there.
      svg.selectAll('*').remove();

      // Add subnode group
      svg.append('g').attr('id', 'mindmap-subnodes');
      this.prepareNodes();

      // Bind data to SVG elements and set all the properties to render them.
      var connections = (0, _d2.d3Connections)(svg, this.props.connections);

      var _d3Nodes = (0, _d2.d3Nodes)(svg, this.props.nodes),
          nodes = _d3Nodes.nodes,
          subnodes = _d3Nodes.subnodes;

      nodes.append('title').text(function (node) {
        return node.note;
      });

      // Bind nodes and connections to the simulation.
      this.state.simulation.nodes(this.props.nodes).force('link').links(this.props.connections);

      if (this.props.editable) {
        this.prepareEditor(svg, connections, nodes, subnodes);
      }

      // Tick the simulation 100 times.
      for (var i = 0; i < 100; i += 1) {
        this.state.simulation.tick();
      }
      (0, _d2.onTick)(connections, nodes, subnodes);

      // Add pan and zoom behavior and remove double click to zoom.
      svg.attr('viewBox', (0, _dimensions.getViewBox)(nodes.data())).call((0, _d2.d3PanZoom)(svg)).on('dblclick.zoom', null);
    }
  }, {
    key: 'componentDidMount',
    value: function componentDidMount() {
      this.renderMap();
    }
  }, {
    key: 'componentDidUpdate',
    value: function componentDidUpdate() {
      (0, _d.zoom)().transform((0, _d.select)(this.refs.mountPoint), _d.zoomIdentity);
      this.renderMap();
    }

    // eslint-disable-next-line class-methods-use-this

  }, {
    key: 'render',
    value: function render() {
      var _this3 = this;

      return _react2.default.createElement(
        'div',
        null,
        _react2.default.createElement('svg', { className: 'mindmap-svg', ref: function ref(input) {
            _this3.mountPoint = input;
          } })
      );
    }
  }]);

  return MindMap;
}(_react.Component);

exports.default = MindMap;


MindMap.defaultProps = {
  nodes: [],
  connections: [],
  editable: false
};

MindMap.propTypes = {
  nodes: _propTypes2.default.array,
  connections: _propTypes2.default.array,
  editable: _propTypes2.default.bool
};

/***/ }),
/* 7 */
/***/ (function(module, exports) {

module.exports = __WEBPACK_EXTERNAL_MODULE_7__;

/***/ }),
/* 8 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

if (process.env.NODE_ENV !== 'production') {
  var REACT_ELEMENT_TYPE = typeof Symbol === 'function' && Symbol.for && Symbol.for('react.element') || 0xeac7;

  var isValidElement = function isValidElement(object) {
    return (typeof object === 'undefined' ? 'undefined' : _typeof(object)) === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
  };

  // By explicitly using `prop-types` you are opting into new development behavior.
  // http://fb.me/prop-types-in-prod
  var throwOnDirectAccess = true;
  module.exports = __webpack_require__(9)(isValidElement, throwOnDirectAccess);
} else {
  // By explicitly using `prop-types` you are opting into new production behavior.
  // http://fb.me/prop-types-in-prod
  module.exports = __webpack_require__(12)();
}
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 9 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var assign = __webpack_require__(10);

var ReactPropTypesSecret = __webpack_require__(1);
var checkPropTypes = __webpack_require__(11);

var printWarning = function printWarning() {};

if (process.env.NODE_ENV !== 'production') {
  printWarning = function printWarning(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };
}

function emptyFunctionThatReturnsNull() {
  return null;
}

module.exports = function (isValidElement, throwOnDirectAccess) {
  /* global Symbol */
  var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
  var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.

  /**
   * Returns the iterator method function contained on the iterable object.
   *
   * Be sure to invoke the function with the iterable as context:
   *
   *     var iteratorFn = getIteratorFn(myIterable);
   *     if (iteratorFn) {
   *       var iterator = iteratorFn.call(myIterable);
   *       ...
   *     }
   *
   * @param {?object} maybeIterable
   * @return {?function}
   */
  function getIteratorFn(maybeIterable) {
    var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
    if (typeof iteratorFn === 'function') {
      return iteratorFn;
    }
  }

  /**
   * Collection of methods that allow declaration and validation of props that are
   * supplied to React components. Example usage:
   *
   *   var Props = require('ReactPropTypes');
   *   var MyArticle = React.createClass({
   *     propTypes: {
   *       // An optional string prop named "description".
   *       description: Props.string,
   *
   *       // A required enum prop named "category".
   *       category: Props.oneOf(['News','Photos']).isRequired,
   *
   *       // A prop named "dialog" that requires an instance of Dialog.
   *       dialog: Props.instanceOf(Dialog).isRequired
   *     },
   *     render: function() { ... }
   *   });
   *
   * A more formal specification of how these methods are used:
   *
   *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
   *   decl := ReactPropTypes.{type}(.isRequired)?
   *
   * Each and every declaration produces a function with the same signature. This
   * allows the creation of custom validation functions. For example:
   *
   *  var MyLink = React.createClass({
   *    propTypes: {
   *      // An optional string or URI prop named "href".
   *      href: function(props, propName, componentName) {
   *        var propValue = props[propName];
   *        if (propValue != null && typeof propValue !== 'string' &&
   *            !(propValue instanceof URI)) {
   *          return new Error(
   *            'Expected a string or an URI for ' + propName + ' in ' +
   *            componentName
   *          );
   *        }
   *      }
   *    },
   *    render: function() {...}
   *  });
   *
   * @internal
   */

  var ANONYMOUS = '<<anonymous>>';

  // Important!
  // Keep this list in sync with production version in `./factoryWithThrowingShims.js`.
  var ReactPropTypes = {
    array: createPrimitiveTypeChecker('array'),
    bool: createPrimitiveTypeChecker('boolean'),
    func: createPrimitiveTypeChecker('function'),
    number: createPrimitiveTypeChecker('number'),
    object: createPrimitiveTypeChecker('object'),
    string: createPrimitiveTypeChecker('string'),
    symbol: createPrimitiveTypeChecker('symbol'),

    any: createAnyTypeChecker(),
    arrayOf: createArrayOfTypeChecker,
    element: createElementTypeChecker(),
    instanceOf: createInstanceTypeChecker,
    node: createNodeChecker(),
    objectOf: createObjectOfTypeChecker,
    oneOf: createEnumTypeChecker,
    oneOfType: createUnionTypeChecker,
    shape: createShapeTypeChecker,
    exact: createStrictShapeTypeChecker
  };

  /**
   * inlined Object.is polyfill to avoid requiring consumers ship their own
   * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
   */
  /*eslint-disable no-self-compare*/
  function is(x, y) {
    // SameValue algorithm
    if (x === y) {
      // Steps 1-5, 7-10
      // Steps 6.b-6.e: +0 != -0
      return x !== 0 || 1 / x === 1 / y;
    } else {
      // Step 6.a: NaN == NaN
      return x !== x && y !== y;
    }
  }
  /*eslint-enable no-self-compare*/

  /**
   * We use an Error-like object for backward compatibility as people may call
   * PropTypes directly and inspect their output. However, we don't use real
   * Errors anymore. We don't inspect their stack anyway, and creating them
   * is prohibitively expensive if they are created too often, such as what
   * happens in oneOfType() for any type before the one that matched.
   */
  function PropTypeError(message) {
    this.message = message;
    this.stack = '';
  }
  // Make `instanceof Error` still work for returned errors.
  PropTypeError.prototype = Error.prototype;

  function createChainableTypeChecker(validate) {
    if (process.env.NODE_ENV !== 'production') {
      var manualPropTypeCallCache = {};
      var manualPropTypeWarningCount = 0;
    }
    function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
      componentName = componentName || ANONYMOUS;
      propFullName = propFullName || propName;

      if (secret !== ReactPropTypesSecret) {
        if (throwOnDirectAccess) {
          // New behavior only for users of `prop-types` package
          var err = new Error('Calling PropTypes validators directly is not supported by the `prop-types` package. ' + 'Use `PropTypes.checkPropTypes()` to call them. ' + 'Read more at http://fb.me/use-check-prop-types');
          err.name = 'Invariant Violation';
          throw err;
        } else if (process.env.NODE_ENV !== 'production' && typeof console !== 'undefined') {
          // Old behavior for people using React.PropTypes
          var cacheKey = componentName + ':' + propName;
          if (!manualPropTypeCallCache[cacheKey] &&
          // Avoid spamming the console because they are often not actionable except for lib authors
          manualPropTypeWarningCount < 3) {
            printWarning('You are manually calling a React.PropTypes validation ' + 'function for the `' + propFullName + '` prop on `' + componentName + '`. This is deprecated ' + 'and will throw in the standalone `prop-types` package. ' + 'You may be seeing this warning due to a third-party PropTypes ' + 'library. See https://fb.me/react-warning-dont-call-proptypes ' + 'for details.');
            manualPropTypeCallCache[cacheKey] = true;
            manualPropTypeWarningCount++;
          }
        }
      }
      if (props[propName] == null) {
        if (isRequired) {
          if (props[propName] === null) {
            return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required ' + ('in `' + componentName + '`, but its value is `null`.'));
          }
          return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required in ' + ('`' + componentName + '`, but its value is `undefined`.'));
        }
        return null;
      } else {
        return validate(props, propName, componentName, location, propFullName);
      }
    }

    var chainedCheckType = checkType.bind(null, false);
    chainedCheckType.isRequired = checkType.bind(null, true);

    return chainedCheckType;
  }

  function createPrimitiveTypeChecker(expectedType) {
    function validate(props, propName, componentName, location, propFullName, secret) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== expectedType) {
        // `propValue` being instance of, say, date/regexp, pass the 'object'
        // check, but we can offer a more precise error message here rather than
        // 'of type `object`'.
        var preciseType = getPreciseType(propValue);

        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + preciseType + '` supplied to `' + componentName + '`, expected ') + ('`' + expectedType + '`.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createAnyTypeChecker() {
    return createChainableTypeChecker(emptyFunctionThatReturnsNull);
  }

  function createArrayOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside arrayOf.');
      }
      var propValue = props[propName];
      if (!Array.isArray(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an array.'));
      }
      for (var i = 0; i < propValue.length; i++) {
        var error = typeChecker(propValue, i, componentName, location, propFullName + '[' + i + ']', ReactPropTypesSecret);
        if (error instanceof Error) {
          return error;
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createElementTypeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      if (!isValidElement(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createInstanceTypeChecker(expectedClass) {
    function validate(props, propName, componentName, location, propFullName) {
      if (!(props[propName] instanceof expectedClass)) {
        var expectedClassName = expectedClass.name || ANONYMOUS;
        var actualClassName = getClassName(props[propName]);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + actualClassName + '` supplied to `' + componentName + '`, expected ') + ('instance of `' + expectedClassName + '`.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createEnumTypeChecker(expectedValues) {
    if (!Array.isArray(expectedValues)) {
      process.env.NODE_ENV !== 'production' ? printWarning('Invalid argument supplied to oneOf, expected an instance of array.') : void 0;
      return emptyFunctionThatReturnsNull;
    }

    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      for (var i = 0; i < expectedValues.length; i++) {
        if (is(propValue, expectedValues[i])) {
          return null;
        }
      }

      var valuesString = JSON.stringify(expectedValues);
      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of value `' + propValue + '` ' + ('supplied to `' + componentName + '`, expected one of ' + valuesString + '.'));
    }
    return createChainableTypeChecker(validate);
  }

  function createObjectOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside objectOf.');
      }
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an object.'));
      }
      for (var key in propValue) {
        if (propValue.hasOwnProperty(key)) {
          var error = typeChecker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
          if (error instanceof Error) {
            return error;
          }
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createUnionTypeChecker(arrayOfTypeCheckers) {
    if (!Array.isArray(arrayOfTypeCheckers)) {
      process.env.NODE_ENV !== 'production' ? printWarning('Invalid argument supplied to oneOfType, expected an instance of array.') : void 0;
      return emptyFunctionThatReturnsNull;
    }

    for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
      var checker = arrayOfTypeCheckers[i];
      if (typeof checker !== 'function') {
        printWarning('Invalid argument supplied to oneOfType. Expected an array of check functions, but ' + 'received ' + getPostfixForTypeWarning(checker) + ' at index ' + i + '.');
        return emptyFunctionThatReturnsNull;
      }
    }

    function validate(props, propName, componentName, location, propFullName) {
      for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
        var checker = arrayOfTypeCheckers[i];
        if (checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret) == null) {
          return null;
        }
      }

      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`.'));
    }
    return createChainableTypeChecker(validate);
  }

  function createNodeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      if (!isNode(props[propName])) {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a ReactNode.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }
      for (var key in shapeTypes) {
        var checker = shapeTypes[key];
        if (!checker) {
          continue;
        }
        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
        if (error) {
          return error;
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createStrictShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }
      // We need to check all keys in case some are required but missing from
      // props.
      var allKeys = assign({}, props[propName], shapeTypes);
      for (var key in allKeys) {
        var checker = shapeTypes[key];
        if (!checker) {
          return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` key `' + key + '` supplied to `' + componentName + '`.' + '\nBad object: ' + JSON.stringify(props[propName], null, '  ') + '\nValid keys: ' + JSON.stringify(Object.keys(shapeTypes), null, '  '));
        }
        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
        if (error) {
          return error;
        }
      }
      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function isNode(propValue) {
    switch (typeof propValue === 'undefined' ? 'undefined' : _typeof(propValue)) {
      case 'number':
      case 'string':
      case 'undefined':
        return true;
      case 'boolean':
        return !propValue;
      case 'object':
        if (Array.isArray(propValue)) {
          return propValue.every(isNode);
        }
        if (propValue === null || isValidElement(propValue)) {
          return true;
        }

        var iteratorFn = getIteratorFn(propValue);
        if (iteratorFn) {
          var iterator = iteratorFn.call(propValue);
          var step;
          if (iteratorFn !== propValue.entries) {
            while (!(step = iterator.next()).done) {
              if (!isNode(step.value)) {
                return false;
              }
            }
          } else {
            // Iterator will provide entry [k,v] tuples rather than values.
            while (!(step = iterator.next()).done) {
              var entry = step.value;
              if (entry) {
                if (!isNode(entry[1])) {
                  return false;
                }
              }
            }
          }
        } else {
          return false;
        }

        return true;
      default:
        return false;
    }
  }

  function isSymbol(propType, propValue) {
    // Native Symbol.
    if (propType === 'symbol') {
      return true;
    }

    // 19.4.3.5 Symbol.prototype[@@toStringTag] === 'Symbol'
    if (propValue['@@toStringTag'] === 'Symbol') {
      return true;
    }

    // Fallback for non-spec compliant Symbols which are polyfilled.
    if (typeof Symbol === 'function' && propValue instanceof Symbol) {
      return true;
    }

    return false;
  }

  // Equivalent of `typeof` but with special handling for array and regexp.
  function getPropType(propValue) {
    var propType = typeof propValue === 'undefined' ? 'undefined' : _typeof(propValue);
    if (Array.isArray(propValue)) {
      return 'array';
    }
    if (propValue instanceof RegExp) {
      // Old webkits (at least until Android 4.0) return 'function' rather than
      // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
      // passes PropTypes.object.
      return 'object';
    }
    if (isSymbol(propType, propValue)) {
      return 'symbol';
    }
    return propType;
  }

  // This handles more types than `getPropType`. Only used for error messages.
  // See `createPrimitiveTypeChecker`.
  function getPreciseType(propValue) {
    if (typeof propValue === 'undefined' || propValue === null) {
      return '' + propValue;
    }
    var propType = getPropType(propValue);
    if (propType === 'object') {
      if (propValue instanceof Date) {
        return 'date';
      } else if (propValue instanceof RegExp) {
        return 'regexp';
      }
    }
    return propType;
  }

  // Returns a string that is postfixed to a warning about an invalid type.
  // For example, "undefined" or "of type array"
  function getPostfixForTypeWarning(value) {
    var type = getPreciseType(value);
    switch (type) {
      case 'array':
      case 'object':
        return 'an ' + type;
      case 'boolean':
      case 'date':
      case 'regexp':
        return 'a ' + type;
      default:
        return type;
    }
  }

  // Returns class name of the object, if any.
  function getClassName(propValue) {
    if (!propValue.constructor || !propValue.constructor.name) {
      return ANONYMOUS;
    }
    return propValue.constructor.name;
  }

  ReactPropTypes.checkPropTypes = checkPropTypes;
  ReactPropTypes.PropTypes = ReactPropTypes;

  return ReactPropTypes;
};
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 10 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/


/* eslint-disable no-unused-vars */

var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var hasOwnProperty = Object.prototype.hasOwnProperty;
var propIsEnumerable = Object.prototype.propertyIsEnumerable;

function toObject(val) {
	if (val === null || val === undefined) {
		throw new TypeError('Object.assign cannot be called with null or undefined');
	}

	return Object(val);
}

function shouldUseNative() {
	try {
		if (!Object.assign) {
			return false;
		}

		// Detect buggy property enumeration order in older V8 versions.

		// https://bugs.chromium.org/p/v8/issues/detail?id=4118
		var test1 = new String('abc'); // eslint-disable-line no-new-wrappers
		test1[5] = 'de';
		if (Object.getOwnPropertyNames(test1)[0] === '5') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test2 = {};
		for (var i = 0; i < 10; i++) {
			test2['_' + String.fromCharCode(i)] = i;
		}
		var order2 = Object.getOwnPropertyNames(test2).map(function (n) {
			return test2[n];
		});
		if (order2.join('') !== '0123456789') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test3 = {};
		'abcdefghijklmnopqrst'.split('').forEach(function (letter) {
			test3[letter] = letter;
		});
		if (Object.keys(Object.assign({}, test3)).join('') !== 'abcdefghijklmnopqrst') {
			return false;
		}

		return true;
	} catch (err) {
		// We don't expect any of the above to throw, but better to be safe.
		return false;
	}
}

module.exports = shouldUseNative() ? Object.assign : function (target, source) {
	var from;
	var to = toObject(target);
	var symbols;

	for (var s = 1; s < arguments.length; s++) {
		from = Object(arguments[s]);

		for (var key in from) {
			if (hasOwnProperty.call(from, key)) {
				to[key] = from[key];
			}
		}

		if (getOwnPropertySymbols) {
			symbols = getOwnPropertySymbols(from);
			for (var i = 0; i < symbols.length; i++) {
				if (propIsEnumerable.call(from, symbols[i])) {
					to[symbols[i]] = from[symbols[i]];
				}
			}
		}
	}

	return to;
};

/***/ }),
/* 11 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var printWarning = function printWarning() {};

if (process.env.NODE_ENV !== 'production') {
  var ReactPropTypesSecret = __webpack_require__(1);
  var loggedTypeFailures = {};

  printWarning = function printWarning(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };
}

/**
 * Assert that the values match with the type specs.
 * Error messages are memorized and will only be shown once.
 *
 * @param {object} typeSpecs Map of name to a ReactPropType
 * @param {object} values Runtime values that need to be type-checked
 * @param {string} location e.g. "prop", "context", "child context"
 * @param {string} componentName Name of the component for error messages.
 * @param {?Function} getStack Returns the component stack.
 * @private
 */
function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
  if (process.env.NODE_ENV !== 'production') {
    for (var typeSpecName in typeSpecs) {
      if (typeSpecs.hasOwnProperty(typeSpecName)) {
        var error;
        // Prop type validation may throw. In case they do, we don't want to
        // fail the render phase where it didn't fail before. So we log it.
        // After these have been cleaned up, we'll let them throw.
        try {
          // This is intentionally an invariant that gets caught. It's the same
          // behavior as without this statement except with a better message.
          if (typeof typeSpecs[typeSpecName] !== 'function') {
            var err = Error((componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' + 'it must be a function, usually from the `prop-types` package, but received `' + _typeof(typeSpecs[typeSpecName]) + '`.');
            err.name = 'Invariant Violation';
            throw err;
          }
          error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
        } catch (ex) {
          error = ex;
        }
        if (error && !(error instanceof Error)) {
          printWarning((componentName || 'React class') + ': type specification of ' + location + ' `' + typeSpecName + '` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a ' + (typeof error === 'undefined' ? 'undefined' : _typeof(error)) + '. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).');
        }
        if (error instanceof Error && !(error.message in loggedTypeFailures)) {
          // Only monitor this failure once because there tends to be a lot of the
          // same error.
          loggedTypeFailures[error.message] = true;

          var stack = getStack ? getStack() : '';

          printWarning('Failed ' + location + ' type: ' + error.message + (stack != null ? stack : ''));
        }
      }
    }
  }
}

module.exports = checkPropTypes;
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 12 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var ReactPropTypesSecret = __webpack_require__(1);

function emptyFunction() {}

module.exports = function () {
  function shim(props, propName, componentName, location, propFullName, secret) {
    if (secret === ReactPropTypesSecret) {
      // It is still safe when called from React.
      return;
    }
    var err = new Error('Calling PropTypes validators directly is not supported by the `prop-types` package. ' + 'Use PropTypes.checkPropTypes() to call them. ' + 'Read more at http://fb.me/use-check-prop-types');
    err.name = 'Invariant Violation';
    throw err;
  };
  shim.isRequired = shim;
  function getShim() {
    return shim;
  };
  // Important!
  // Keep this list in sync with production version in `./factoryWithTypeCheckers.js`.
  var ReactPropTypes = {
    array: shim,
    bool: shim,
    func: shim,
    number: shim,
    object: shim,
    string: shim,
    symbol: shim,

    any: shim,
    arrayOf: getShim,
    element: shim,
    instanceOf: getShim,
    node: shim,
    objectOf: getShim,
    oneOf: getShim,
    oneOfType: getShim,
    shape: getShim,
    exact: getShim
  };

  ReactPropTypes.checkPropTypes = emptyFunction;
  ReactPropTypes.PropTypes = ReactPropTypes;

  return ReactPropTypes;
};

/***/ }),
/* 13 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.d3PanZoom = exports.d3Drag = exports.onTick = exports.d3Nodes = exports.d3Connections = undefined;

var _d = __webpack_require__(2);

var _dimensions = __webpack_require__(3);

/*
 * Bind data to a <TAG> tag, inside a G element, inside the given root element.
 * Root is a D3 selection, data is an object or array, tag is a string.
 */
var bindData = function bindData(root, data, tag) {
  return root.append('g').selectAll(tag).data(data).enter().append(tag);
};

/*
 * Bind connections to PATH tags on the given SVG.
 */
var d3Connections = exports.d3Connections = function d3Connections(svg, connections) {
  return bindData(svg, connections, 'path').attr('class', 'mindmap-connection');
};

/* eslint-disable no-param-reassign */
/*
 * Bind nodes to FOREIGNOBJECT tags on the given SVG,
 * and set dimensions and html.
 */
var d3Nodes = exports.d3Nodes = function d3Nodes(svg, nodes) {
  var selection = svg.append('g').selectAll('foreignObject').data(nodes).enter();

  var d3nodes = selection.append('foreignObject').attr('class', 'mindmap-node').attr('width', function (node) {
    return node.width + 4;
  }).attr('height', function (node) {
    return node.height;
  }).html(function (node) {
    return node.html;
  });

  var d3subnodes = selection.append('foreignObject').attr('class', 'mindmap-subnodes').attr('width', function (node) {
    return node.nodesWidth + 4;
  }).attr('height', function (node) {
    return node.nodesHeight;
  }).html(function (subnode) {
    return subnode.nodesHTML;
  });

  return {
    nodes: d3nodes,
    subnodes: d3subnodes
  };
};

/*
 * Callback for forceSimulation tick event.
 */
var onTick = exports.onTick = function onTick(conns, nodes, subnodes) {
  var d = function d(conn) {
    return ['M', conn.source.x, conn.source.y, 'Q', conn.source.x + (conn.curve && conn.curve.x ? conn.curve.x : 0), conn.source.y + (conn.curve && conn.curve.y ? conn.curve.y : 0), ',', conn.target.x, conn.target.y].join(' ');
  };

  // Set the connections path.
  conns.attr('d', d);

  // Set nodes position.
  nodes.attr('x', function (node) {
    return node.x - node.width / 2;
  }).attr('y', function (node) {
    return node.y - node.height / 2;
  });

  // Set subnodes groups color and position.
  subnodes.attr('x', function (node) {
    return node.x + node.width / 2;
  }).attr('y', function (node) {
    return node.y - node.nodesHeight / 2;
  });
};

/*
 * Return drag behavior to use on d3.selection.call().
 */
var d3Drag = exports.d3Drag = function d3Drag(simulation, svg, nodes) {
  var dragStart = function dragStart(node) {
    if (!_d.event.active) {
      simulation.alphaTarget(0.2).restart();
    }

    node.fx = node.x;
    node.fy = node.y;
  };

  var dragged = function dragged(node) {
    node.fx = _d.event.x;
    node.fy = _d.event.y;
  };

  var dragEnd = function dragEnd() {
    if (!_d.event.active) {
      simulation.alphaTarget(0);
    }

    svg.attr('viewBox', (0, _dimensions.getViewBox)(nodes.data()));
  };

  return (0, _d.drag)().on('start', dragStart).on('drag', dragged).on('end', dragEnd);
};
/* eslint-enable no-param-reassign */

/*
 * Return pan and zoom behavior to use on d3.selection.call().
 */
var d3PanZoom = exports.d3PanZoom = function d3PanZoom(el) {
  return (0, _d.zoom)().scaleExtent([0.3, 5]).on('zoom', function () {
    return el.selectAll('svg > g').attr('transform', _d.event.transform);
  });
};

/***/ }),
/* 14 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _emojis = __webpack_require__(4);

/*
 * Return the HTML representation of a node.
 * The node is an object that has text, url, and category attributes;
 * all of them optional.
 */
var subnodesToHTML = function subnodesToHTML() {
  var subnodes = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var fcolor = arguments[1];

  var color = fcolor || '';

  if (!fcolor && subnodes.length > 0 && subnodes[0].color) {
    color = 'style="border-left-color: ' + subnodes[0].color + '"';
  }

  return subnodes.map(function (subnode) {
    var href = 'href="' + subnode.url + '"';
    var emoji = (0, _emojis.categoryToIMG)(subnode.category);

    if (!subnode.url) {
      href = '';
      emoji = '';
    }

    return '<div class="mindmap-subnode-group" ' + color + '>\n      <a ' + href + '>' + (subnode.text || '') + ' ' + emoji + '</a>\n      <div>' + subnodesToHTML(subnode.nodes, color) + '</div>\n    </div>';
  }).join('');
};

exports.default = subnodesToHTML;

/***/ }),
/* 15 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _emojis = __webpack_require__(4);

/*
 * Return the HTML representation of a node.
 * The node is an object that has text, url, and category attributes;
 * all of them optional.
 */
exports.default = function (node) {
  var href = 'href="' + node.url + '"';
  var emoji = (0, _emojis.categoryToIMG)(node.category);

  // If url is not specified remove the emoji and the href attribute,
  // so that the node isn't clickable, and the user can see that without
  // having to hover the node.
  if (!node.url) {
    href = '';
    emoji = '';
  }

  return '<a id="node-' + node.index + '" ' + href + '>' + (node.text || '') + ' ' + emoji + '</a>';
};

/***/ }),
/* 16 */
/***/ (function(module, exports, __webpack_require__) {

// style-loader: Adds some css to the DOM by adding a <style> tag

// load the styles
var content = __webpack_require__(17);
if(typeof content === 'string') content = [[module.i, content, '']];
// Prepare cssTransformation
var transform;

var options = {}
options.transform = transform
// add the styles to the DOM
var update = __webpack_require__(19)(content, options);
if(content.locals) module.exports = content.locals;
// Hot Module Replacement
if(false) {
	// When the styles change, update the <style> tags
	if(!content.locals) {
		module.hot.accept("!!../node_modules/css-loader/index.js!../node_modules/postcss-loader/lib/index.js!../node_modules/sass-loader/lib/loader.js!./main.sass", function() {
			var newContent = require("!!../node_modules/css-loader/index.js!../node_modules/postcss-loader/lib/index.js!../node_modules/sass-loader/lib/loader.js!./main.sass");
			if(typeof newContent === 'string') newContent = [[module.id, newContent, '']];
			update(newContent);
		});
	}
	// When the module is disposed, remove the <style> tags
	module.hot.dispose(function() { update(); });
}

/***/ }),
/* 17 */
/***/ (function(module, exports, __webpack_require__) {

exports = module.exports = __webpack_require__(18)(false);
// imports


// module
exports.push([module.i, ".mindmap-svg {\n  height: 100vh;\n  width: 100%; }\n  .mindmap-svg:focus {\n    outline: none; }\n\n.mindmap-node > a {\n  background: #f5f5f5;\n  border-radius: 10px;\n  -webkit-box-shadow: 0 2px 2px 0 rgba(0, 0, 0, 0.14), 0 3px 1px -2px rgba(0, 0, 0, 0.2), 0 1px 5px 0 rgba(0, 0, 0, 0.12);\n          box-shadow: 0 2px 2px 0 rgba(0, 0, 0, 0.14), 0 3px 1px -2px rgba(0, 0, 0, 0.2), 0 1px 5px 0 rgba(0, 0, 0, 0.12);\n  color: #212121;\n  display: inline-block;\n  font-family: 'Raleway';\n  font-size: 22px;\n  margin: 0 auto;\n  padding: 15px;\n  text-align: center;\n  text-decoration: none;\n  -webkit-transition: background-color .2s, color .2s ease-out;\n  transition: background-color .2s, color .2s ease-out; }\n  .mindmap-node > a[href]:hover {\n    background-color: #f57c00;\n    color: #fff;\n    cursor: pointer; }\n\n.mindmap-node--editable {\n  cursor: all-scroll; }\n  .mindmap-node--editable > a {\n    pointer-events: none; }\n\n.mindmap-subnode-group {\n  -webkit-box-align: center;\n      -ms-flex-align: center;\n          align-items: center;\n  border-left: 4px solid #9e9e9e;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  margin-left: 15px;\n  padding: 5px; }\n  .mindmap-subnode-group a {\n    color: #212121;\n    font-family: 'Raleway';\n    font-size: 16px;\n    padding: 2px 5px; }\n\n.mindmap-connection {\n  fill: transparent;\n  stroke: #9e9e9e;\n  stroke-dasharray: 10px 4px;\n  stroke-width: 3px; }\n\n.mindmap-emoji {\n  height: 24px;\n  vertical-align: bottom;\n  width: 24px; }\n\n.reddit-emoji {\n  border-radius: 50%; }\n", ""]);

// exports


/***/ }),
/* 18 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/
// css base code, injected by the css-loader
module.exports = function (useSourceMap) {
	var list = [];

	// return the list of modules as css string
	list.toString = function toString() {
		return this.map(function (item) {
			var content = cssWithMappingToString(item, useSourceMap);
			if (item[2]) {
				return "@media " + item[2] + "{" + content + "}";
			} else {
				return content;
			}
		}).join("");
	};

	// import a list of modules into the list
	list.i = function (modules, mediaQuery) {
		if (typeof modules === "string") modules = [[null, modules, ""]];
		var alreadyImportedModules = {};
		for (var i = 0; i < this.length; i++) {
			var id = this[i][0];
			if (typeof id === "number") alreadyImportedModules[id] = true;
		}
		for (i = 0; i < modules.length; i++) {
			var item = modules[i];
			// skip already imported module
			// this implementation is not 100% perfect for weird media query combinations
			//  when a module is imported multiple times with different media queries.
			//  I hope this will never occur (Hey this way we have smaller bundles)
			if (typeof item[0] !== "number" || !alreadyImportedModules[item[0]]) {
				if (mediaQuery && !item[2]) {
					item[2] = mediaQuery;
				} else if (mediaQuery) {
					item[2] = "(" + item[2] + ") and (" + mediaQuery + ")";
				}
				list.push(item);
			}
		}
	};
	return list;
};

function cssWithMappingToString(item, useSourceMap) {
	var content = item[1] || '';
	var cssMapping = item[3];
	if (!cssMapping) {
		return content;
	}

	if (useSourceMap && typeof btoa === 'function') {
		var sourceMapping = toComment(cssMapping);
		var sourceURLs = cssMapping.sources.map(function (source) {
			return '/*# sourceURL=' + cssMapping.sourceRoot + source + ' */';
		});

		return [content].concat(sourceURLs).concat([sourceMapping]).join('\n');
	}

	return [content].join('\n');
}

// Adapted from convert-source-map (MIT)
function toComment(sourceMap) {
	// eslint-disable-next-line no-undef
	var base64 = btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap))));
	var data = 'sourceMappingURL=data:application/json;charset=utf-8;base64,' + base64;

	return '/*# ' + data + ' */';
}

/***/ }),
/* 19 */
/***/ (function(module, exports, __webpack_require__) {

/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/

var stylesInDom = {};

var	memoize = function (fn) {
	var memo;

	return function () {
		if (typeof memo === "undefined") memo = fn.apply(this, arguments);
		return memo;
	};
};

var isOldIE = memoize(function () {
	// Test for IE <= 9 as proposed by Browserhacks
	// @see http://browserhacks.com/#hack-e71d8692f65334173fee715c222cb805
	// Tests for existence of standard globals is to allow style-loader
	// to operate correctly into non-standard environments
	// @see https://github.com/webpack-contrib/style-loader/issues/177
	return window && document && document.all && !window.atob;
});

var getElement = (function (fn) {
	var memo = {};

	return function(selector) {
		if (typeof memo[selector] === "undefined") {
			memo[selector] = fn.call(this, selector);
		}

		return memo[selector]
	};
})(function (target) {
	return document.querySelector(target)
});

var singleton = null;
var	singletonCounter = 0;
var	stylesInsertedAtTop = [];

var	fixUrls = __webpack_require__(20);

module.exports = function(list, options) {
	if (typeof DEBUG !== "undefined" && DEBUG) {
		if (typeof document !== "object") throw new Error("The style-loader cannot be used in a non-browser environment");
	}

	options = options || {};

	options.attrs = typeof options.attrs === "object" ? options.attrs : {};

	// Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
	// tags it will allow on a page
	if (!options.singleton) options.singleton = isOldIE();

	// By default, add <style> tags to the <head> element
	if (!options.insertInto) options.insertInto = "head";

	// By default, add <style> tags to the bottom of the target
	if (!options.insertAt) options.insertAt = "bottom";

	var styles = listToStyles(list, options);

	addStylesToDom(styles, options);

	return function update (newList) {
		var mayRemove = [];

		for (var i = 0; i < styles.length; i++) {
			var item = styles[i];
			var domStyle = stylesInDom[item.id];

			domStyle.refs--;
			mayRemove.push(domStyle);
		}

		if(newList) {
			var newStyles = listToStyles(newList, options);
			addStylesToDom(newStyles, options);
		}

		for (var i = 0; i < mayRemove.length; i++) {
			var domStyle = mayRemove[i];

			if(domStyle.refs === 0) {
				for (var j = 0; j < domStyle.parts.length; j++) domStyle.parts[j]();

				delete stylesInDom[domStyle.id];
			}
		}
	};
};

function addStylesToDom (styles, options) {
	for (var i = 0; i < styles.length; i++) {
		var item = styles[i];
		var domStyle = stylesInDom[item.id];

		if(domStyle) {
			domStyle.refs++;

			for(var j = 0; j < domStyle.parts.length; j++) {
				domStyle.parts[j](item.parts[j]);
			}

			for(; j < item.parts.length; j++) {
				domStyle.parts.push(addStyle(item.parts[j], options));
			}
		} else {
			var parts = [];

			for(var j = 0; j < item.parts.length; j++) {
				parts.push(addStyle(item.parts[j], options));
			}

			stylesInDom[item.id] = {id: item.id, refs: 1, parts: parts};
		}
	}
}

function listToStyles (list, options) {
	var styles = [];
	var newStyles = {};

	for (var i = 0; i < list.length; i++) {
		var item = list[i];
		var id = options.base ? item[0] + options.base : item[0];
		var css = item[1];
		var media = item[2];
		var sourceMap = item[3];
		var part = {css: css, media: media, sourceMap: sourceMap};

		if(!newStyles[id]) styles.push(newStyles[id] = {id: id, parts: [part]});
		else newStyles[id].parts.push(part);
	}

	return styles;
}

function insertStyleElement (options, style) {
	var target = getElement(options.insertInto)

	if (!target) {
		throw new Error("Couldn't find a style target. This probably means that the value for the 'insertInto' parameter is invalid.");
	}

	var lastStyleElementInsertedAtTop = stylesInsertedAtTop[stylesInsertedAtTop.length - 1];

	if (options.insertAt === "top") {
		if (!lastStyleElementInsertedAtTop) {
			target.insertBefore(style, target.firstChild);
		} else if (lastStyleElementInsertedAtTop.nextSibling) {
			target.insertBefore(style, lastStyleElementInsertedAtTop.nextSibling);
		} else {
			target.appendChild(style);
		}
		stylesInsertedAtTop.push(style);
	} else if (options.insertAt === "bottom") {
		target.appendChild(style);
	} else {
		throw new Error("Invalid value for parameter 'insertAt'. Must be 'top' or 'bottom'.");
	}
}

function removeStyleElement (style) {
	if (style.parentNode === null) return false;
	style.parentNode.removeChild(style);

	var idx = stylesInsertedAtTop.indexOf(style);
	if(idx >= 0) {
		stylesInsertedAtTop.splice(idx, 1);
	}
}

function createStyleElement (options) {
	var style = document.createElement("style");

	options.attrs.type = "text/css";

	addAttrs(style, options.attrs);
	insertStyleElement(options, style);

	return style;
}

function createLinkElement (options) {
	var link = document.createElement("link");

	options.attrs.type = "text/css";
	options.attrs.rel = "stylesheet";

	addAttrs(link, options.attrs);
	insertStyleElement(options, link);

	return link;
}

function addAttrs (el, attrs) {
	Object.keys(attrs).forEach(function (key) {
		el.setAttribute(key, attrs[key]);
	});
}

function addStyle (obj, options) {
	var style, update, remove, result;

	// If a transform function was defined, run it on the css
	if (options.transform && obj.css) {
	    result = options.transform(obj.css);

	    if (result) {
	    	// If transform returns a value, use that instead of the original css.
	    	// This allows running runtime transformations on the css.
	    	obj.css = result;
	    } else {
	    	// If the transform function returns a falsy value, don't add this css.
	    	// This allows conditional loading of css
	    	return function() {
	    		// noop
	    	};
	    }
	}

	if (options.singleton) {
		var styleIndex = singletonCounter++;

		style = singleton || (singleton = createStyleElement(options));

		update = applyToSingletonTag.bind(null, style, styleIndex, false);
		remove = applyToSingletonTag.bind(null, style, styleIndex, true);

	} else if (
		obj.sourceMap &&
		typeof URL === "function" &&
		typeof URL.createObjectURL === "function" &&
		typeof URL.revokeObjectURL === "function" &&
		typeof Blob === "function" &&
		typeof btoa === "function"
	) {
		style = createLinkElement(options);
		update = updateLink.bind(null, style, options);
		remove = function () {
			removeStyleElement(style);

			if(style.href) URL.revokeObjectURL(style.href);
		};
	} else {
		style = createStyleElement(options);
		update = applyToTag.bind(null, style);
		remove = function () {
			removeStyleElement(style);
		};
	}

	update(obj);

	return function updateStyle (newObj) {
		if (newObj) {
			if (
				newObj.css === obj.css &&
				newObj.media === obj.media &&
				newObj.sourceMap === obj.sourceMap
			) {
				return;
			}

			update(obj = newObj);
		} else {
			remove();
		}
	};
}

var replaceText = (function () {
	var textStore = [];

	return function (index, replacement) {
		textStore[index] = replacement;

		return textStore.filter(Boolean).join('\n');
	};
})();

function applyToSingletonTag (style, index, remove, obj) {
	var css = remove ? "" : obj.css;

	if (style.styleSheet) {
		style.styleSheet.cssText = replaceText(index, css);
	} else {
		var cssNode = document.createTextNode(css);
		var childNodes = style.childNodes;

		if (childNodes[index]) style.removeChild(childNodes[index]);

		if (childNodes.length) {
			style.insertBefore(cssNode, childNodes[index]);
		} else {
			style.appendChild(cssNode);
		}
	}
}

function applyToTag (style, obj) {
	var css = obj.css;
	var media = obj.media;

	if(media) {
		style.setAttribute("media", media)
	}

	if(style.styleSheet) {
		style.styleSheet.cssText = css;
	} else {
		while(style.firstChild) {
			style.removeChild(style.firstChild);
		}

		style.appendChild(document.createTextNode(css));
	}
}

function updateLink (link, options, obj) {
	var css = obj.css;
	var sourceMap = obj.sourceMap;

	/*
		If convertToAbsoluteUrls isn't defined, but sourcemaps are enabled
		and there is no publicPath defined then lets turn convertToAbsoluteUrls
		on by default.  Otherwise default to the convertToAbsoluteUrls option
		directly
	*/
	var autoFixUrls = options.convertToAbsoluteUrls === undefined && sourceMap;

	if (options.convertToAbsoluteUrls || autoFixUrls) {
		css = fixUrls(css);
	}

	if (sourceMap) {
		// http://stackoverflow.com/a/26603875
		css += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))) + " */";
	}

	var blob = new Blob([css], { type: "text/css" });

	var oldSrc = link.href;

	link.href = URL.createObjectURL(blob);

	if(oldSrc) URL.revokeObjectURL(oldSrc);
}


/***/ }),
/* 20 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * When source maps are enabled, `style-loader` uses a link element with a data-uri to
 * embed the css on the page. This breaks all relative urls because now they are relative to a
 * bundle instead of the current page.
 *
 * One solution is to only use full urls, but that may be impossible.
 *
 * Instead, this function "fixes" the relative urls to be absolute according to the current page location.
 *
 * A rudimentary test suite is located at `test/fixUrls.js` and can be run via the `npm test` command.
 *
 */

module.exports = function (css) {
	// get current location
	var location = typeof window !== "undefined" && window.location;

	if (!location) {
		throw new Error("fixUrls requires window.location");
	}

	// blank or null?
	if (!css || typeof css !== "string") {
		return css;
	}

	var baseUrl = location.protocol + "//" + location.host;
	var currentDir = baseUrl + location.pathname.replace(/\/[^\/]*$/, "/");

	// convert each url(...)
	/*
 This regular expression is just a way to recursively match brackets within
 a string.
 	 /url\s*\(  = Match on the word "url" with any whitespace after it and then a parens
    (  = Start a capturing group
      (?:  = Start a non-capturing group
          [^)(]  = Match anything that isn't a parentheses
          |  = OR
          \(  = Match a start parentheses
              (?:  = Start another non-capturing groups
                  [^)(]+  = Match anything that isn't a parentheses
                  |  = OR
                  \(  = Match a start parentheses
                      [^)(]*  = Match anything that isn't a parentheses
                  \)  = Match a end parentheses
              )  = End Group
              *\) = Match anything and then a close parens
          )  = Close non-capturing group
          *  = Match anything
       )  = Close capturing group
  \)  = Match a close parens
 	 /gi  = Get all matches, not the first.  Be case insensitive.
  */
	var fixedCss = css.replace(/url\s*\(((?:[^)(]|\((?:[^)(]+|\([^)(]*\))*\))*)\)/gi, function (fullMatch, origUrl) {
		// strip quotes (if they exist)
		var unquotedOrigUrl = origUrl.trim().replace(/^"(.*)"$/, function (o, $1) {
			return $1;
		}).replace(/^'(.*)'$/, function (o, $1) {
			return $1;
		});

		// already a full url? no change
		if (/^(#|data:|http:\/\/|https:\/\/|file:\/\/\/)/i.test(unquotedOrigUrl)) {
			return fullMatch;
		}

		// convert the url to a full url
		var newUrl;

		if (unquotedOrigUrl.indexOf("//") === 0) {
			//TODO: should we add protocol?
			newUrl = unquotedOrigUrl;
		} else if (unquotedOrigUrl.indexOf("/") === 0) {
			// path should be relative to the base url
			newUrl = baseUrl + unquotedOrigUrl; // already starts with '/'
		} else {
			// path should be relative to current directory
			newUrl = currentDir + unquotedOrigUrl.replace(/^\.\//, ""); // Strip leading './'
		}

		// send back the fixed url(...)
		return "url(" + JSON.stringify(newUrl) + ")";
	});

	// send back the fixed css
	return fixedCss;
};

/***/ })
/******/ ]);
});